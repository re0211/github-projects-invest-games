# 跨平台游戏制作 × AI 开发资源梳理（2026-09-24 · 第二十辑）

> 搜索覆盖：GitHub / Gitee（`gh api` 逐仓库实测 stars·forks·license·language·pushed，**无一估算**）/
> CSDN · 稀土掘金 · 今日头条 · 腾讯云开发者社区 · 游侠网 · 量子位 · 个人博客 /
> **厂商官网与官方 release notes**（Anthropic · Unity · GitHub · VS Code · MLCommons）/
> **海外社区**（Hacker News · r/LocalLLaMA · DEV Community · note.com · GameDev.net）/
> **榜单**（Artificial Analysis · The AI Changelog · MLPerf）
> —— 第三路（官方 + 海外）原派出的子 agent 因 429 中断且未落盘，**本辑由主 agent 直接补跑**。
>
> 上一辑（第十九辑）的主线是**「省下来的 vs 赔进去的」** —— 压缩工具输出是纯赚，
> 压缩模型的说话方式和行为分布要赔。**本辑顺着「谁来判断」往下追一层。**
>
> **本辑主线 = 「判据外移」。**
> 第十九辑我们从 `alibaba/open-code-review` 学到一句：**错不起的步骤应该确定性化**。
> 本辑有六份互相独立的材料在说同一件事 —— **外移的不该只是"步骤"，还有规格、记忆、
> 证据、以及"压不压"这件事本身的裁决权**：
> ① 一次 150+ commit 的复盘把边界钉进**词表**，让"字段名偏离"变成可机器检查的 bug；
> ② `dsh-argp` 让 LLM **只提议**逐原子压缩，**确定性守卫**负责裁决；
> ③ `guardvibe` 把 472 条安全规则做成**不调模型**的本地层；
> ④ `model-serving-minefield` 按**症状**索引部署地雷 —— 和我们的 `mistakes/` 完全同构；
> ⑤ 一篇中文长文给出**五个外部验证测试**，让"你的记忆系统到底行不行"第一次可测；
> ⑥ 我们自己照着跑了一遍（见 §六），**一次抓出 3 个真缺陷**。
>
> 已有索引：`index.html`（**1112** 项 → 本辑 **第二十六版增补 8 张卡 → 1120**）
> + 前十九辑经验帖（累计约 605 条 + 本辑 **约 60 条** ≈ **665 条**）
> + `game-production-pipeline.md`（决策原则 **54** 条 → 本辑 **58**）
> + `agent-house-rules.md`（**42** 条房规 → 本辑 **47**）+ `mistakes/`（**19** → **21** 条）
>
> 上一辑存档：`csdn-social-summary-v19.md`

---

## 本辑一句话

**凡是能让机器判的，就别让模型判；凡是让模型判的，就要有能把它判错的测试。**
前半句我们已经在做（六道 `check_*.py`），后半句是本辑的增量 ——
**我们第一次给自己的记忆系统和闸门做了反例测试，而且一次就抓到东西。**

---

## 一、规格外移：把边界钉进词表（本辑最高价值的一篇）

### 1. Vibe Coding 上线之后：一次 150+ commit 的复盘
https://hanyouqing.com/blog/2026/09/when-vibe-coding-hits-production
个人博客 · Youqing Han（DevOps Engineer） · **2026-09-14**

一个多服务集成功能：**第一周能演示，修到生产可用断断续续一个多月，累计 150+ commits，
其中约六成是 fix**（回归、边界、字段名对不上、并发坑）。核心原则一句话：
**AI 写出来的代码，质量上限 ≈ 你给它的规格有多完整。**

六类翻车（每条都是"模型猜了本该查的东西"）：
① **接口契约漂移** —— `user_identifier` / `user_id` / `userId` 三处不同名（**AI 没有跨会话记忆**）
② 外部系统边角料（空字符串被拒、锁名最长 64、非法 ID 抛 validation 而非 not-found）
③ 内部基建隐性约定（非生产环境审批跳步、异步任务要等事务提交后再发）
④ **并发语义想当然**（`update()` 返回"匹配 1 行"≠改了 1 行；行锁挡不住两个并发 insert 同时通过唯一性检查）
⑤ 状态机只写成功路径（超时但其实成功了，副作用不对齐）
⑥ 前后端字段靠猜

**可抄的产物 —— 词表（Vocabulary table）**：
`Concept | DB field | API field | External system name | Constraints | Notes` 六列，
**确认后当不可变**，生成代码里字段名偏离词表的先改再往下走。配套的**带副作用矩阵的状态机**。

**Phase 分会话**：Phase 0（开 AI 前）约束必须来自一手来源，**用 SDK 源码或小实验验证，别只问 AI**；
Phase 1 单开 session 只出规格、**禁止写实现**；Phase 2 每个 session 只做一个可验收单元；
Phase 3 联调前先 `curl` 拿一份**真实响应 JSON 贴进 PR**，前端以它为权威类型。

收益粗算：规格不充分时约**近百个 fix commit**，每个 fix 端到端约 45 分钟 → 几十人时；
Phase 0 + 设计会话通常只要几小时。

→ **已落地为决策原则 #55（词表即契约）**。这也是第十九辑待办 #1「架构边界闸门」的**更上游答案**：
与其事后检测"共享函数签名被改"，不如**事前把名字钉成表，让偏离当场就是 bug**。
去重核查：`hanyouqing.com` 域名在三份基线里**零命中**。

---

## 二、记忆外移：五道外部验证测试（本辑我们真跑了的那一篇）

### 2. Agent 都说自己有长期记忆？四层记忆管道的设计真相与五个验证测试
https://blog.csdn.net/qq_40140450/article/details/164714386
CSDN · 白日放歌须纵久（qq_40140450） · **2026-09-09**

**四层管道（成本递增、价值递增）**：①工作记忆（上下文窗口）②对话历史（会话日志/摘要）
③**持久记忆（文件：`CLAUDE.md` / `AGENTS.md`，显式写入、人可读、可 git 追踪、可 code review）**
④长程记忆（向量库/知识图谱，读写都在黑盒，可审计性弱）。
作者的核心指责：**把第 1/2 层包装成第 3/4 层卖给用户，是市场乱象的来源。**

**五个外部验证测试（本条最值钱）**：
1. **延迟召回**：会话 A 教一条偏好 → **24 小时后**开会话 B 不重提。三档判定：直接复述（可能是检索碰巧命中）/
   **行为正确且能溯源到记忆条目（最理想）**/行为错误。
2. **写入路径审计**：任务结束后立刻 `git diff` / 目录比对 —— **宣称记住必须有物理落盘，一条都没落盘直接判负**。
3. **读取链路**：人为注入一条决定性规则并写明"任务启动必须引用经验编号"，看**行为对 + 回复里出现编号**
   （只做对一半 = 读取链路不可靠，正确只是偶然）。
4. **记忆漂移**：第 1 天和第 15 天各让 Agent 复述一次，对照是否稳定或有序演化。
5. **抗污染**：在目录 A 创设"仅适用于 A"的记忆，在目录 B 给相似任务，**B 不得误用 A 的记忆**。

配套的极简自建架构：**3 个文件 + 1 个目录约定**；经验条目格式
`L-042 | 日期 | 现象 | 根因 | 解法 | 复用条件`；`lessons.md` **超过 200 条启动归档**。

→ **已落地：见 §六，我们照着写了 `_tools/memory_health.py` 并真跑了一遍。**

### 3. AI Agent 记忆系统设计：从短期记忆到长期记忆的工程实践
https://blog.csdn.net/weixin_29051811/article/details/164900274
CSDN · weixin_29051811 · **2026-09-10**

**参数全部给了数字的一篇**：中文场景 `bge-m3` 比 `text2vec` / `text-embedding-3-small` 在长文本上更稳；
**Chunk 200–500 字符**；**Top-K 先取 5–8 条 + 相似度阈值过滤掉低于 0.6 的**；
混合检索 **BM25 + 向量加权 0.6/0.4**。

**编程 Agent 的三层标配**：项目级长期记忆（仓库结构与规范）/ 任务级工作记忆（当前分支状态 → **JSON 文件**）/
会话级（diff 上下文）。
两个排查实录：①**记忆污染**（库里同时有"预算十万"和"预算二十万"，召回到旧的）→ 写入时冲突检测 +
召回时把 `created_at` 一起给模型 + System Prompt 明写"记忆可能过时，以用户当前表述为准"；
②**召回不准** → 按性价比排序：Top-K 5→10 配阈值 → 混合检索 → **最后才换更强的 Embedding**；
**写入时打 metadata 标签，召回先按类型过滤再算相似度，效果提升非常明显**。
迁移格式建议：**JSONL 固定字段 `id`/`user_id`/`content`/`metadata`/`created_at`/`updated_at`**。

→ **只取两条**：metadata 标签 + 冲突检测（已进 `memory_health.py` 的 R6）；向量路线**不采用**
（我们是文件型记忆，`mistakes/` 已能 grep，无需引入 embedding）。

### 4. 从 CLAUDE.md 到本地记忆层 —— 四种方案对比
https://juejin.cn/post/7685745420586385423
稀土掘金 · 大卫AI赋能自媒体 · **2026-09-16**（⚠️ 文末是自家产品推广，**方法可抄、产品不采用**）

一句定义性的话：**前三种方案的本质问题相同 —— 把「记忆」当成了「输入」，而记忆应该是可被查询的存储。**
外部记忆层四职责：**record（判断什么值得记，全存等于没存）/ recall / consolidation（合并重复、淘汰过期）/
file-bridge**。**consolidation 是我们明确的缺口** —— `mistakes/` 只增不合并。

### 5. `mnemoverse/mcp-memory-server` —— 25★ / MIT / TypeScript：召回可以打"误导"分
https://github.com/mnemoverse/mcp-memory-server
创建 2026-09 · 最近推送 2026-09-23

跨 Claude Code / Cursor / VS Code / ChatGPT 的持久记忆 MCP。**新机制：用户可反馈某条召回
"有帮助 / 误导"，系统据此更新排序** —— 这是普通向量检索没有的**负反馈通道**。
→ **不采用**（开源范围只是客户端/MCP server，核心记忆引擎默认托管，自托管仅 Enterprise，
与"本地资料优先"冲突）；**只取机制**：给 `mistakes/` 的每条记录加"上次召回是有用还是误导"的反馈位
（已写进下一轮待办）。

---

## 三、上下文外移：压缩次数、检查点边界、以及"谁裁决"

### 6. 【claude code 实践】Claude Code 高级上下文管理
https://blog.csdn.net/l35633/article/details/163804073
CSDN · l35633 · **2026-08-18**

三层压缩机制：**MicroCompact**（规则驱动、不调模型、删过长工具输出与合并重复）/
**AutoCompact（剩余空间低于约 13,000 token 时触发**，调模型做摘要）/ Full Compact（九段式摘要）。
**主动压缩阈值：50–70% 就手动 `/compact`**，别等自动。压缩后上下文回落到 20–30%。

**一条具体的退化观察（本条最值钱）**：**经过 2–3 次压缩后，模型读完整文件的能力显著下降，
转为用 grep 和部分读取"采样"文件内容。** 缓解：在 `CLAUDE.md` 里写明"压缩时必须保留的内容"。
`/compact` 可附加保留指令（`/compact 保留认证模块的架构决策和已确认的 API 格式`）。

→ **已落地为房规 #43（压缩次数是独立维度）**：我们房规 #34/#38 只讲预算，**没有"压缩次数"这个维度**。

### 7. 几轮对话后 Claude 变笨了
https://juejin.cn/post/7668906037449965574
稀土掘金（原文 time-friend.com） · **2026-08-01**

**每发出一条 prompt 就自动创建一个快照**，不只记对话，还**快照被修改的文件状态**；
`/rewind` 可选「只回退对话 / 只恢复代码 / 两者都回退 / 从某节点开始压缩」。

⚠️ **一条明确的边界（我们没记过）**：**检查点只追踪模型通过编辑工具做的修改，
通过 Bash 命令或外部进程改的东西不在此列** —— 它不是 git 的替代品。
→ **对我们是直接风险**：我们大量用 Bash 跑 `insert_v*_cards.py`、`_tools/*.py`，
**误以为有快照兜底是有风险的**（已写进房规 #43）。

一个反例：**模型反复回到一个错误假设时，`/compact` 会把那个错误假设也保留下来**，
正确做法是 `/clear` + 更精确的 prompt。土办法：**每完成一个逻辑单元就 `git commit` → `/clear` → 再开下一个**。

### 8. `yoza10635/dsh-argp` —— DSH 双阶段上下文压缩：LLM 只提议，守卫裁决
https://github.com/yoza10635/dsh-argp
**9★ / MIT / TypeScript** · 创建 2026-09 · 最近推送 2026-09-23

**LLM 只提出逐原子压缩，确定性守卫负责裁决**；再用 **0-LLM 引用图剪枝**，
并从 append-only 日志按摘要或原文**精确召回**。默认 npm 安装只启用 Stage-2 的 0-LLM 图剪
（对 16GB 本地机成本最低），需要更强压缩才启 Stage-1。

→ **判据价值 > 工具价值**：**"提议"给模型、"裁决"给代码**这个切法，
比"要不要压缩"这个二选一更有用。已写进决策原则 #56。
⚠️ 9★ 且依赖 DSH，`dsh` 仍在 rc（本机 `0.1.0-rc.6`）→ **只登记不装**（M-0002）。

### 9. `rossoctl/context-guru` —— 56★ / Apache-2.0 / Go：让上下文预算变成可观测的
https://github.com/rossoctl/context-guru

Provider-agnostic 上下文工程代理层：少携带上下文、检索对话轨迹、压缩与摘要，
并提供**状态与统计观察面**。
→ **参考**：我们已经有 `_tools/harness_tax_probe.py`（实测 20,900 token），
缺的是**每次任务实时的水位**，可抄它的"预设裁剪策略 + 统计面"。安装路径偏 Claude Code 插件，
对本地 Ollama 与现用 harness 要额外适配 → 不装。

### 10. Token 成本优化实战：caveman 压缩 65% 输出 + 长上下文「虚假」陷阱
https://blog.csdn.net/2301_81443758/article/details/162551204
CSDN · 2301_81443758 · **2026-07-03**

**⚠️ 本辑唯一一处必须对冲的矛盾**（详见 §七）：这篇说 caveman 平均省 65%、且引论文称
"限制输出简短在某些基准上 **+26 个百分点**"；而第十九辑 `context-mode` #482 因为 Moonshot
在 kimi-k2.5 上的报告**删掉了 22 处同类注入**。**两边都有数字、结论相反。**

**处置：不采用它的"压文风"部分**（房规 #40 已判，且 #40 的证据是"项目自己删功能"这种**行为证据**，
强于自评基准）；**只取不冲突的三条**：
① **生产有效上下文 ≈ 广告窗口的 25–30%**（1M 窗口模型超过 200K 后就不再像 1M 模型；
具体建议上限：Opus 4.8 1M → 280K、Gemini 2 Flash 1M → 300K、GPT-5 200K → 50K）
② **连贯干扰 > 随机干扰** → 检索块要用 `## Source N` **原始拼接**，不要改写成流畅摘要
③ **Lost-in-the-Middle 位置评测法**：取 30 个真实问题，把答案块放在第 1/5/10/15/20 位测准确率，
**掉 >20pt 就是上下文太长**（答案在位置 5–15 时准确率下降 30+ 个百分点）

### 11. 上下文工程与子智能 —— 「1M 窗口是危险的创可贴」
https://juejin.cn/post/7685963804388458506
稀土掘金 · AI研究社_ · **2026-09-16**（⚠️ 文末带企业推广，**软文嫌疑，只取方法不取结论**）

可引用但**无出处**的对比：全上下文方案准确率 **72.9% / 延迟 17 秒 / 每会话 26,000 token**；
选择性记忆检索准确率约 **66–67% / 延迟降 91% / token 减 90%**。

**可取的两句判据**：① **记忆提升（promotion）要设三级闸门** —— 会话→用户级允许；
会话→租户级**只允许已验证事实**；会话→全局级**运行时绝不执行**。
② **子智能体输出当作"带出处的工具结果"，不是继承父级权威的指令**（失败模式叫"确认偏误注入"）。
→ 已写进房规 #44。

---

## 四、游戏制作 × 市场底数

### 12. 我用 AI 一个月做完第一款游戏《撞个大西瓜》：5.5 万行代码、软著两周过审
https://juejin.cn/post/7673458070442475546
稀土掘金 · 萝卜啊 · **2026-08-14**

规模：**234 个文件 / 5.5 万行 TypeScript**，微信小游戏。
**版本钉子（本条最值钱）**：**PixiJS 必须锁 6，不能升 7/8** —— 微信小游戏 WebGL 只支持 **GLSL 100**，
PixiJS 7+ 部分 shader 升到 GLSL 300 直接跑不起来 → **锁死 6.5.10，并在 `AGENTS.md` 里写死"绝不升级"**。
其余钉子：微信 JS 引擎不支持 `?.` `??`（`tsconfig` 目标 es2017）、不支持 ESM（rollup 收成 CJS 单文件）、
**主包 4MB 硬限制**、软著材料里卡通骷髅图形会被打回。

**五条 AI 分工线**：代码 = DeepSeek V4 Flash（日常）+ V4 Pro（改三次还不对的 bug）；
方案推演 = **MiniMax M3（100 万上下文，需求 + 整个仓库一起读，先出方案和风险清单）**；
美术 = ChatGPT Image 2.0；语音 = MiniMax；**音效 = LLM 直接用 WebAudio 写合成代码**
（参数可调、风格统一、零版权风险 —— **这一招对我们 Ren'Py 线直接可用**）。

一句方法论：**模型换谁都能换，质量门禁不能省**；**AI 每踩一个坑就沉淀一条规则进 `AGENTS.md`，
这是整个项目复利最大的动作。**
→ **已落地：§八 给 `AGENTS.md` 加了「版本钉子」表**（钉子 = 版本号 + 为什么不能动，光写版本号没用）。

### 13. 让 AI 写你的视觉小说：renpy-mcp（Gitee 版，第五个实现）
https://juejin.cn/post/7671572326089949194 ｜ 仓库 https://gitee.com/gdouage/renpy-mcp （MIT）
稀土掘金 · 啊b的开发日记 · **2026-08-08**

**15 个工具 / 7 大类**：读代码 4（`list_labels` rich 模式看参数与 jump/call 目标、`read_script`、
`find_references`、`list_definitions`）／写 1（`exec_rpy`，**8 种定位模式**）／
编译验证 3（`compile_project`/`lint_project`/`check_project`）／自动修复 2／存档管理／素材 2／
**文档搜索 2（`search_docs` 内置 23 页 Ren'Py 中文官方文档，580KB，离线可搜）**。

**auto_fix 覆盖 5 类经典坑**：`return_values`（label 里写 `return True` 会崩）、`missing_from`
（`call xxx` 缺 `from` 子句）、**`bom_normalize`（有中文的文件没 BOM 会漏掉第一行 label）**、
`missing_labels`、`stale_saves`。

⚠️ **必须和我们第十一辑的实测对冲**：`Muanchen2/renpy-mcp` 的"CJK 自动配字体"**对我们有害**
（把合规的思源黑体换成商业字体 simhei，M-0008）。**同一个功能名在不同实现上结论相反。**
→ **处置：只启用 `search_docs`（零风险）+ `find_references`（只读），BOM/字体类自动修复一律不开。**

### 14. 外媒：AI 加剧游戏供给过剩（Steam 市场底数）
https://3g.ali213.net/news/html/1039439.html
游侠网转 4Gamer 专栏 · **2026-09-14**（⚠️ 二手转载、无一手链接，**只作方向判断，不写进决策原则**）

- 2026 年前 5 个月 Steam 上线 **9,265 款**新游戏，销量破百万的爆款仅 **14 款，占 0.15%**
- 2026 上半年 Steam **新作总营收同比减少 3.9 亿美元**；**中小游戏销售额中位数仅数百美元**
- Steam 强制披露 AI 后，**每月上新游戏中约三分之一标注用到 AI**
- 玩家态度：**各年龄段反对 AI 的约三成**；**5 美元及以下抵触弱，超过 20 美元显著变严苛**
- **愿望单机制失效**：普通独立游戏**愿望单转首周购买只有 10–15%**
- **Valve 已改算法**：更看重**上线后长期游玩活跃度与好评**，扶持长尾

→ **给下一轮待办的一条建议**：第十九辑待办 #5「活跃的开源 Steam 上架/发行工具（四次未闭合）」
优先级**建议下调** —— 本轮中文侧仍然没找到工具，而 #14 给出的是**反方向证据**：
愿望单转化率已降到 10–15%，瓶颈不在"上架工具"而在"有没有人玩"。

### 15. 耗时一周，AI 复刻童年扫雷
https://m.toutiao.com/article/7677221048589074954
今日头条 · **无作者/无精确日期**（移动端页面）

**这一周里纯编码时间可能只占 30%，剩下 70% 全在解决 AI 搞不定的"脏活累活"。**
后期短板：①**接口适配滞后** —— AI 会用已下线/废弃的微信开放能力（全网排行榜只能改成本地单机榜）；
②**限流打断心流** —— Kimi 频繁 429，每次中断留下一堆未完成残缺代码。
微信暗礁：**权限配置缺失**（头像上传第二次报错，排查半天发现不是代码问题）、
**HTTPS 必须 A 级以上，Let's Encrypt 免费证书过不了审**。
→ **"AI 会用废弃 API"这条对我们是直接风险**（Ollama / Ren'Py SDK 版本差异同理）
→ 已写进房规 #44「给 AI 的上下文里必须包含当前 SDK 版本」。

---

## 五、GitHub 本辑其余条目（逐条 `gh api` 实测，零估算）

| # | 仓库 | ★ / 协议 / 语言 | 一句话 | 判断 |
|---|---|---|---|---|
| 16 | [`KyosukeIshizu1008/berryscode`](https://github.com/KyosukeIshizu1008/berryscode) | 145 / MIT / Rust | Bevy 原生 IDE：场景编辑 + ECS Inspector + System Graph + 可接 Ollama 的本地 AI | 参考（Bevy 向，范式可借） |
| 17 | [`chrisgliddon/bevy-skills`](https://github.com/chrisgliddon/bevy-skills) | 15 / MIT / Python | Bevy 0.19 可移植 Agent Skills，**每段示例都用配套仓库编译校验** | 参考（"示例必须能编译"这条可抄） |
| 18 | [`indiesoftby/defold-agent-config`](https://github.com/indiesoftby/defold-agent-config) | 95 / 未声明 / Python | Defold 的 `AGENTS.md` 样板：依赖源码下载到**只读 `.deps/`**、单任务会话、**65% 上下文上限** | 参考（JIT 拉依赖 + 只读上下文） |
| 19 | [`striderZA/OpenCodeGameStudios`](https://github.com/striderZA/OpenCodeGameStudios) | 88 / MIT / JS | 52 角色 / 22 模块 / **158 项测试**，按引擎包按需装载 | 参考（只抽策划·叙事·实现·QA 四角色） |
| 20 | [`hatayama/unity-cli-loop`](https://github.com/hatayama/unity-cli-loop) | 568 / MIT / C# | CLI 驱动 Unity：编译→测试→日志→场景→**截图→输入回放→断点** 全闭环 | 参考（"最少工具覆盖闭环"可映射到 Ren'Py） |
| 21 | [`godot-fun/gai`](https://github.com/godot-fun/gai) | 170 / MIT / GDScript | Godot Agent CLI + 跨 harness 技能集：音视频/图片/分镜批处理 + **不可覆盖源文件约束** | 参考（素材管线约束可移植） |
| 22 | [`coddy-project/coddy-agent`](https://github.com/coddy-project/coddy-agent) | 154 / MIT / Go | 单静态二进制 harness：TUI/ACP/Web/定时任务 + **项目可信门**（阻止仓库自带 hook 自动执行） | 落地候选（有 Windows 二进制 + Ollama 后端） |
| 23 | [`dcellison/kai`](https://github.com/dcellison/kai) | 36 / Apache-2.0 / Python | 常驻编码工作台，**SQLite append-only event log** 存会话/任务/记忆 | 参考（append-only 日志思路与 `mistakes/` 同构） |
| 24 | [`unbound-force/dewey`](https://github.com/unbound-force/dewey) | 3 / MIT / Go | Logseq/Obsidian + 代码库知识图谱 MCP，**Ollama 本地向量化** | 落地候选（3★ 太新，先观察） |
| 25 | [`gary23w/nl-veil`](https://github.com/gary23w/nl-veil) | 204 / MIT / Zig | Windows 原生桌面编码 Agent，跨会话项目记忆，内置/Ollama/OpenAI 三选一 | 落地候选（内置 12B 会挤 16GB，换小模型） |
| 26 | [`ShenSeanChen/waku-agent`](https://github.com/ShenSeanChen/waku-agent) | **1,825** / MIT / Python | 刻意可读可改的本地优先骨架：loop + 三类记忆 + **检索门 + 确定性测试 + LLM-as-judge + 发布闸门** | 落地候选（本批星最高，`pip install` 可跑） |
| 27 | [`skymecode/deepseek-harness-for-vscode`](https://github.com/skymecode/deepseek-harness-for-vscode) | 148 / MIT / TS | **DSH 做成原生 VS Code 工作台**，内置 runtime，会话持久化/分叉/导入导出、文件上下文卡 | 落地候选（有 VSIX + 简体中文，无需另装 DSH CLI） |
| 28 | [`NERDSORG/Mutsumi`](https://github.com/NERDSORG/Mutsumi) | 20 / Apache-2.0 / TS | VS Code 多 Agent：notebook 存纯文本会话、**上下文预览**、文件版本/hash 跟踪、**过期引用裁剪** | 参考（JIT 上下文装配适合 `index.html`） |
| 29 | [`goklab/guardvibe`](https://github.com/goklab/guardvibe) | 5 / Apache-2.0 / TS | vibe-coded Web 项目的**本地确定性安全层**：**472 条规则 / 39 MCP 工具**、`npx guardvibe` 无账号 | 落地候选（Web 栈向，不能替 Ren'Py 的 Bandit） |
| 30 | [`Blackwellboy/model-serving-minefield`](https://github.com/Blackwellboy/model-serving-minefield) | 134 / 未声明 / Python | 按**症状**整理的模型部署/评测地雷证据库（chat template / tool parser / 量化 kernel / KV，**含 Ollama 专页**） | 参考（与 `mistakes/` **完全同构**，是"按症状索引"的样板） |
| 31 | [`ukanwat/selfstarter`](https://github.com/ukanwat/selfstarter) | 386 / MIT / Shell | "明确 brief + 预算 + 完成条件"交给 Agent 跨会话自驱（附独立做 UE5 开放世界实验） | 不采用（绑定 UE5，仓库 405MB） |

**剔除记录**：`gamedev-skills/awesome-gamedev-agent-skills`、`wellingfeng/UltraGameStudio`、
`IvanMurzak/Godot-MCP`、`CoplayDev/unity-mcp`、`hi-godot/godot-ai` —— `index.html` 已收录；
`IvanMurzak/Unity-MCP`、`hybridindie/godot-mcp` 属已高密度覆盖的同类，不堆叠；
一批 0–1★ 同模板空壳（`vibecheck` / `vibesafe` / `protectvibe` 等）与 4 个 laya/dsh 压缩变体（只留机制最清楚的 `dsh-argp`）。

---

## 六、本辑自测：把五道验证测试跑在自己的 `mistakes/` 上（✅ 已改）

**这是本辑唯一"改了"的部分**，也是第二十辑待办 #6「量一次自己闸门的召回率」的**第一次真正执行**。

照着 §二 #2 的五个测试 + §二 #3 的冲突检测，写了 **`_tools/memory_health.py`**（六道检查，零依赖）：

| 检查 | 内容 | 第一次跑 |
|---|---|---|
| R1 | 落盘对账：`index.json` 的每个 id ↔ yml 文件（**双向**） | ✅ |
| R2 | 字段完整：`symptom`/`rootCause`/`preferredPattern`/`retrievalKeys` | ❌ **抓到 2 条** |
| R3 | 复审期限：`expiresAfter` 已过 → 提醒 | ✅（0 条到期） |
| R4 | 读取链路：4 个真实检索探针 → 期望命中条目 | ❌ **抓到 1 条** |
| R5 | 抗污染：每条必须声明适用范围（`scope` 或 `paths`） | ✅ |
| R6 | 冲突检测：两条 `errorStrings` 重叠 ≥60% → 疑似重复 | ✅（0 组） |

**抓到的 3 个真缺陷（全部已修）**：
1. **M-0010 / M-0011 用的是旧模板**（只有 `bannedPattern`/`preferredPattern`，缺 `symptom`/`rootCause`）
   → 已按现行模板补齐两节，并注明补写原因。
   **这两条是 2026-09-20 写的，躺了 4 天没人发现 —— 因为从来没有人检查过模板一致性。**
2. **M-0020 的检索词「召回」只有 2 个字，检索链路抓不住**（R4 探针命中不到）
   → 已换成可命中的长词（`召回率 3/3` / `只做反例不做正例` / `四格表只填了一格`）。
   **教训：检索键有长度下限，太短的键等于没有键。**
3. 顺带确认：**`mistakes/` 目前没有一条过期、没有一组重复**（R3/R6 全绿）。

**另外一处"测量工具自己也有缺陷"**：第一版 `memory_health.py` 把文件名 `M-0001-xxx.yml`
按 `-` 切分取第一段，得到 `M` → 20 条全被判"没落盘"。**量闸门的尺子本身也要校准**
→ 已写进 **M-0020**（第 20 条错误记忆）。

**配套**：同批新建 **`_tools/gate_recall_probe.py`**（闸门召回探针，专测 `skill_budget.py`）。
**它第一次跑也是假警** —— 判据写成"技能名出现在输出里"，而闸门会把**合规技能也打在表里**。
改成"定位该技能那一行、看行尾有没有判定标记"后：**3 条缺陷全抓到 + 负对照 0 假警**。
→ **核心教训（M-0020）：只量召回率的探针会把「什么都报」当成「抓得全」——
测闸门必须正例反例一起跑，召回率是一个四格表，不是一个数。**

---

## 七、本辑必须记下的两处"口径相反"

### 7.1 caveman vs 房规 #40（同一件事，两个方向都有数字）
- 第十九辑：`context-mode` #482 —— Moonshot 报告激进简洁提示损害基准 → **删掉 22 处注入**
- 本辑 §三 #10：caveman 称平均省 65%，并引论文称限制输出简短**在某些基准上 +26 个百分点**

**处置**：不采用 caveman 的压文风部分（#40 的证据是"项目自己删功能"这种**行为证据**，强于自评基准）；
只取不冲突的三条（25–30% 有效窗口 / `## Source N` 原始拼接 / Lost-in-the-Middle 位置评测法）。
**并把"两个来源结论相反"这件事本身记下来** —— 与 M-0019「数字一样，口径不同」是同族，
这次是**同一件事、两个方向都有数字**。

### 7.2 已收条目内部的矛盾也要标注
`renpy-mcp` 的 BOM/字体自动修复：Gitee 版（本辑 #13）说有用，
我们第十一辑实测 GitHub 版（#Muanchen2）**有害**。**同一个功能名在不同实现上结论相反**
→ 处置写进条目本身（只启用 `search_docs` + `find_references`）。

---

## 八、索引与文档维护（本辑）

- `index.html` **第二十六版增补 8 张卡**（**1112 → 1120**，脚注 **[1112]-[1119]**），脚本 `insert_v26_cards.py`
- `agent-house-rules.md` 42 → **47 条**（+#43 压缩次数是独立维度 / #44 子智能体输出＝带出处的工具结果 /
  #45 给 AI 的上下文里必须写当前 SDK 版本 / +#46 外部内容不得直写记忆 / +#47 长任务产出边做边落盘）
- `game-production-pipeline.md` 54 → **58 条**（+#55 词表即契约 / #56 提议给模型、裁决给代码 /
  #57 记忆条目要有复用条件字段 / +#58 失败记忆优先）
  ⚠️ **勘误（2026-09-24 01:3x 补记）**：#55~#57 上一轮**只写在汇总里，文件实际没落盘** ——
  当时 `git status --short` 里根本没有 `game-production-pipeline.md` 这一行。本轮已经真正补写。
  → 记为 **M-0021「自述已改 ≠ 已落盘」**，并定了对账动作：**收尾前 `git status --short` 与报告里的"已改"清单逐项对齐**。
- `mistakes/` 19 → **21 条**（+M-0020 只量召回不量假警 / +M-0021 自述已改≠已落盘）
- `D:\34498\Documents\AGENTS.md` 103 → **118 行**：新增 **§十「版本钉子与命名约定」**
  （钉子 = 版本号 + 为什么不能动；四行钉子表 + 命名约定）
- 新增工具：`_tools/memory_health.py` · `_tools/gate_recall_probe.py`
- 原始调研输出：`_r20/_r20_gh.md`（GitHub 路 20 条）· `_r20/_r20_cn.md`（中文社媒路 21 条）
  · `_r20/_r20_official.md`（**官方 + 海外路 19 条，本轮补跑**）

---

## 九、下一辑待办

**本辑新增**
1. **给 `mistakes/` 加"召回反馈"位**（§二 #5 mnemoverse 的机制）：每条记录"上次召回是有用还是误导"，
   并按反馈调检索排序 —— 我们现在是只写不评
2. **给 `mistakes/` 加 `scope` 字段 + consolidation**（§二 #4）：现在是只增不合并，M-0020 之后会更多
3. **Lost-in-the-Middle 位置评测法跑一次自己的记忆**（§三 #10）：30 个真实问题 × 5 个位置，掉 >20pt 就是太长
4. **试 `ShenSeanChen/waku-agent`**（1,825★，pip 可装）或 `coddy-agent`（Windows 二进制 + Ollama）——
   二选一，不要都装
5. **词表落地**：给索引站建一张 `概念 | HTML class | 页头字段 | 脚注编号 | 约束` 表，
   让"卡片数/页头数/脚注数三个口径不一致"这类历史坑变成可检查项

**承接**
6. **架构边界闸门**（Frankenstein 效应）—— 本辑给的是更上游的答案（词表），**闸门本身仍未建**
7. `rpycdec` 对 1.9 发行包反编译自查（欠四轮）
8. Codex 本地 `config.yaml` → Ollama 纯离线
9. DSH 上下文插件二轮评估（等脱离 rc）；`deepseek-harness-for-vscode` 有 VSIX，可作为替代路线先看
10. ~~活跃开源 Steam 上架/发行工具~~ **建议下调优先级**（§四 #14：愿望单转化率已降到 10–15%，瓶颈不在工具）
11. 中文社媒本轮**未找到** DSH 的一手深度使用帖（只有临时分享链接与中转商文档），下轮补

**补录路新增（2026-09-24 01:3x）**
12. **做 `_tools/memory_dream.py`**：输入 `mistakes/*.yml` + 近 N 天日志 → 输出 `mistakes/_dream/YYYY-MM-DD.yml` 供人审
    （抄 Claude Dreaming 的形状：读"会话原文 + 现有记忆"两份、**产出写新文件、旧的不动**、可带 ≤4096 字符的整合指令）
13. **给 `mistakes/` 加"为什么"字段**（§十 10.1 第 5 条）：现在 `preferredPattern` 写的是"怎么做"，
    而实验数据显示**能跨任务迁移的是原因**；顺手把条目格式改成带内容哈希（`content_sha256`）的**不可变版本**
14. **调研类子 agent 的prompt模板加一句"先写文件骨架、之后增量追加"**（本辑 429 事故的直接教训，见房规 #47）
15. **把"先例检查四问"做成开工前的固定动作**（§十 10.6）：学名 / 行内人在用什么 / 为什么现成的不合用 / 能建在它上面的最小版本

---

## 十、官方与海外侧补录（19 条，全量见 `_r20/_r20_official.md`）

> 第三路子 agent 因 429 中断且**未落盘**，本路改由主 agent 直接跑（约 6 次检索）。
> 去重同样是**逐条 grep 四份基线**，本路剔掉 7 条已收/同源。

### 10.1 记忆与上下文：五份材料收敛到同一句话
**"记忆要有版本、要被审、失败比成功值钱。"**

| # | 来源 | 关键数字 | 对我们的意义 |
|---|---|---|---|
| 1 | **Claude Dreaming**（Anthropic，功能发布 2026-05-06） | 读**最多 100 条会话** + 现有 store → 产出**全新 store**（去重 / 替换过期 / 提模式），**原 store 不动**；Harvey 称任务完成率 **~6×** | 待办 #2 的现成参数（产出写新文件、旧的保留可回滚） |
| 2 | **Memory Stores**（Managed Agents，2026-04-23 beta） | 记忆 = `/mnt/memory` 目录；单条 **≤100 KB（~25K token）**；版本不可变留 **30 天**；并发写用 **`content_sha256`** 做乐观锁；Rakuten 首错 **−97%** / 成本 **−27%** / 延迟 **−34%** | **房规 #46 的来源**：⚠️ 默认 `read_write` → **不可信输入会被写进记忆、下轮当可信资料读** |
| 3 | **Projects 重构**（Anthropic，09-17 beta） | 协调者 + **并行线程**（每线程 = 一条云会话 + 独立分支）+ 项目记忆 + Library | 我们三路并行**缺协调者** —— 本轮的 429 就是代价 |
| 4 | **Claude smart reports**（09-10 beta，Enterprise） | 报表要指出**哪些重复模式值得打包成共享 skill**、会话**在哪撞墙** | 给"该不该建新技能"一个判据：同一模式一月内手工做过 ≥3 次 |
| 5 | **r/LocalLLaMA 失败记忆实验**（Rakuen 0.4.0） | 失败记忆**可跨任务泛化**、成功记忆迁移有限；**小上下文 + 记忆 > 大上下文 + 无记忆** | **决策原则 #58** —— 我们"只记错误"第一次有了实验证据 |

### 10.2 上下文工程：两个可算的数
- **半百万 token 仍需要一个索引**（dev.to）：双 3090 跑 Qwen3.8-27B / **524k 上下文 / 60–88 tok/s**。
  推算全部压给上下文是**线性成本**（prefill 读全量 + KV 全驻 + 每 token 全重读，**哪怕答案只要三段**）；
  被忽略的一层是**embedding 存储**：768 维 f32 = **3,072 字节/条 → 百万块 3.07 GB**，5-bit 量化后 **642 MB（4.78×）/ recall@10 0.979 / 3.21 ms**。
  **反例也要记**：某欧洲银行对照实验中，**单文档简单查询上长上下文准确率高 34%** —— 别一刀切。
- **kihaya 的上下文预算算法**（note.com，llama.cpp 实测）：分配 **131,072**，**实际每轮只送 7,000–15,000（10–15%）**，50 轮与第 10 轮无差别。四条：
  ①**不总结历史**，改为**每轮重读工作树当前内容**、旧工具结果**只替换内容**；②**不变项在前、变化项在后**（prompt cache 才生效，内容相同必须字符串相同）；
  ③超预算删"**最近没碰的**"而非"最大的"，且**不真删 —— 缩成只剩定义行 + 行号的骨架**；④到阈值**一次性砍到下限**，之后只追加，**绝不从头截断**。
  战果：棋引擎 52 轮 / 3h28m / 17 测试 / perft 五组数字全对 / **0 次事实性错误**。

### 10.3 本地模型部署的显存算术
- 单张 **4090** + Qwen3.8-27B（**Q4_K_S**）：上下文 **144,369** + **KV cache Q8_0（K/V 都压）** + **Flash Attention 开** + **65 层全 offload** = **23.80 GB / 24 GB**（顺序是倒推的）。
  harness 用 **DSH minimal mode**（工具与调用更少 → 省上下文；标准模式 144k 常干到一半撞墙），**thinking effort = medium**，**55 t/s**；7–8 小时做出一个 Three.js 浏览器游戏。
- 我们可用的部分：**"倒推分配"的方法** + **关思考** + **少工具档位**；不可照搬的是 4090 与 24 GB。

### 10.4 榜单与价格：口径问题占了头条
- **AA Intelligence Index 升到 v4.3**（09-07）：换 **Terminal-Bench 4.0**（66 个多步任务）与 **AutomationBench-AA**（657 条工作流），**评测 harness 从 Terminus 2 换成 mini-SWE-agent**。显示分并列 **53** 的两家，**每任务成本 $3.26 vs $7.63（差 57%）**；官方明说**跨版本分数不可直接比** → 这是 **M-0019「同一个数、不同口径」的又一例**，凡引用 AA 分数必须带版本号。
- **"便宜"要看每任务成本**：Grok 4.7 单价**没涨**（$2/$6），但因为输出量翻倍，**每任务 $2.73 vs 4.6 的 $1.86（+47%）**。
- **DeepSeek 09-10 撤回 8 月的 4 倍涨价**：退役 V4-Flash → **V4.1-Flash**（谷 $0.15/$0.60、峰 $0.30/$1.20，当日放 MIT 权重）；性价比位次仍在 **GLM 5.3 Flash**（**$0.25/41.9 分 vs DeepSeek $0.27/39.5 分**）→ **选型更新：批处理默认换成 GLM 5.3 Flash 或 V4.1-Flash 谷时价，谷时窗口是新杠杆。**
- **MLPerf Inference v6.1** 首设 **Agentic Inference** 基准；一句有用的判断：**高综合分不等于多步循环里好用**。

### 10.5 游戏 × 官方：引擎厂自己下场了
- **Unity 09-10 出 Claude Code 官方插件、09-16 出 Codex 官方插件**（第一方，非第三方魔改）：**31 项**引擎技能（UI Toolkit/uGUI、2D 与瓦片图、URP 与 Shader Graph、音频、导航物理、IAP、联机、Web、本地化）+ `unity-cli`；**结果返回前先过验证流程**；声明用当前 API；支持 **Unity 6+**；两条命令装完，无需逐项目配置。
- 同为官方：**Unity AI Assistant**（Unity 6.0+，编辑器内 Ask/Plan/Agent 三模式 + MCP Server，走 Cloud AI Gateway；Personal 版 14 天 1,000 credits 试用后 **$10/月**）、**Unity 6 自带 AI 生成占位音频**。
- **对我们的意义**：①"官方 schema 优先于模型记忆" = 我们的**版本钉子**同一件事；②"返回前先验证"我们 Ren'Py 侧早就在做（lint + test + 截图）。

### 10.6 vibe coding：社区侧的一手材料
- **Bend 2 与"先例检查"**（HN 603 分 + 回应 323 分）：Bend 2 首页用 **58 行**陈述"玩家碰不到旗子"、用 **442 行 AI 写的证明**去确立，而"形式化验证"一词全站未出现；换 **SPARK**（40 年前就在做这个的语言）重做，体积只剩零头；README 自陈**编译器 99% 由 AI 写成、未完整审计**。
  机制一句话：**构建变便宜了，阅读没有，中间没有闸门** —— 过去"构建的成本本身买了教育"，现在这个税没了。
  处方 = **15 分钟四问闸门**：学名？行内人在用什么？现成方案为什么不合用？**能建在它上面的最小版本是什么？**
- **5,000 行没测过的 AI PR**（HN 49604816）：**AI 生成的测试也可能是假的，给出虚假的安全感** → 直接对应我们这轮的真实教训（`memory_health.py` 第一版把 `M-0001-x.yml` 切出个 `M`，20 条全判"没落盘"）：**闸门脚本必须自己复算一遍**。
- **Agentic coding 长文**（09-07，HN 583 分帖做引子）：最危险的用法是**用 LLM 写规格/文档，再把 AI 产物当事实基准部署**（"规格里就带着模型的自信臆造"）；经验法则 **"输出需要的验证强度与它影响的事物的关键性成正比"**。

### 10.7 编辑器侧（只收前辑未覆盖的）
- **VS Code 1.136**（09-14）：**Agent Host** 独立进程跑 harness（Copilot / Claude / Codex，基于开放 **AHP** 协议）→ **会话属于进程不属于窗口**（关掉文件夹还在、可跨客户端、可多窗口同看）；支持**多根工作区**会话（**hooks 仍限定单文件夹**，多文件夹时提示选主文件夹）；实验性 **Rubber Duck：让互补模型对当前 agent 的工作给第二意见**。
- **GitHub Copilot 周报**（09-18）：评审**能用 shell 工具真跑一遍**验证改动（不只读 diff）；**Lite 评审合并多个 agent 的发现**；已处理的评论自动关闭 + 应用建议时顺手生成 commit message；Copilot 应用新增 **Sentry 画布**（崩溃报告 → 定位 → 验证 → PR）。⚠️ 顺带一个诚实的观察：VS Code 的 release notes 页面自己标注**"本页由 Copilot 生成，可能不准确"**。

**本路剔除 7 条**：Claude Opus 5.5 / GPT-6 Sol·Luna / Grok 4.7 / 小米 MiMo-V2.6（均已在 v18 或 v19 收录）；
VS Code 1.138·1.139 明细与"Auto 三档"（v19 已收，只留 1.136）；"Vibe Coding 90 天猝死定律"（v18 已收且属中文侧）；
Unity 三条中文二传稿（同一事件的多站改写，已知重复形态 ③/⑤）。

---

*本辑统计*：新增约 **60 条**信源（中文社媒 21 / GitHub·Gitee 20 / **官方与海外 19**）→ 累计约 **665 条**；
`index.html` **1120** 项（+8）；`agent-house-rules.md` **47** 条（+5）；
决策原则 **58** 条（+4）；`mistakes/` **21** 条（+2）；
新建工具 2 个（`memory_health.py` / `gate_recall_probe.py`），自测抓出并修掉 **3 个真缺陷**，
另**对账抓出 1 处"报告说已改、文件没改"**（→ M-0021）。
