# Agent 房规（House Rules）

> 来源：Grok Bot Field Notes（xAI 72h 直播现场规则，MIT）+ AI Coding Mastery 六法 + GLM Infra Agent 博客 + OpenAI 失准事件披露 + hqwc 三可判据，2026-09-20 蒸馏
> 用法：给子 agent / 协作 agent 派活时，把相关条款写进 prompt；给自己干活时，当 checklist 用
> 原则：**验证就是工作，写代码是容易的部分。** 不能演示"改动有效"，就没完工。

---

## A. 动手之前（写码/改文件前）

1. **先复现，后修复**：跑一遍，找到确切的 bug 和确切的行为。复现不是形式——它是"你是否真理解了问题"的测试。（Grok Bot：复现是唯一的理解检验）
2. **用自己的话重述任务**：说清楚要做什么、**不做什么**、做了什么假设。重述错了，现在改是免费的，后面改是昂贵的。
3. **一句话说清"看到什么算完成"**：这句话就是之后要附的证明。（对应"计划优先三问"：改哪些文件？各改什么？风险与边缘在哪？确认前不写码。）
4. **只调查不修复时就老实调查**："别开 PR，先说说你怎么看"就是字面意思——别顺手改。

## B. 动手之中

5. **小改动**：一个 diff 一个关切。"全做了"的大 diff 无法验证也无法评审。
6. **跑真的东西**：不是 type checker，是真实应用/游戏/服务。点一遍，故意弄坏它。
7. **修根因不修症状**：准备写 workaround 时，明说"这是 workaround，真修在范围外"。
8. **有现成库就别手搓**：有公认标准工具就用它，别因为没人点名就手搓一个更差的版本。
9. **别编造内容**：需要真实数据（真实名字/接口/端点）就去查。"看起来合理的占位符"发到用户手里比报错更糟。（OpenAI 披露事件：模型失败后编造数据——这是真实发生过的事）
10. **内部语言不泄漏到用户可见输出**：原型名、代号、TODO 措辞、自己的推理过程——检查用户看到的字符串。
11. **两击规则**：同一形状失败两次 = 方向错了，不是参数错了。别试第三个变体，回到第一步重新理解。（来源：apk-reverse skill）
12. **反馈环境是瓶颈，不是模型**：给的任务要配**客观可验证的局部信号**（测试/断言/数值前后对比），别给"整体做漂亮"这种端到端软指标。（来源：z.ai GLM Infra Agent 博客）

## C. 完成标准（交付/合入前）

13. **每次附证明**：
    - UI/视觉改动 → 截图或短录屏
    - 性能/后端 → 前后数字对比（真实运行）
    - bug 修复 → 复现步骤 + 同样步骤现在通过
    - 关卡/内容 → 求解器/自动测试全绿
    - 美术/观感 → 尽量几何化断言（距离/包围盒/颜色剖面），"数据没错"≠"看起来对"
14. **对抗性验证**：重要改动完成后，开一个**新上下文**专门挑毛病（"你是审计员：找安全漏洞/边缘情况/性能瓶颈/与架构不一致处"），不信自己刚写的会话。（来源：CIV 模式）
15. **发布/权限/资金类动作 = 人工闸门**：不管流程跑得多顺，push 生产/改权限/花钱 永远留一个人工确认点。（OpenAI 6 起失准事件里有"未授权写入"——闸门不是不信任，是制度）

## D. 上下文与规则卫生

16. **上下文预算**：大上下文 = 低信号。规则文件/项目指南 **≤200 行**，只写 AI 推断不出的内容；每个项目维护一份 Master Guideline（组件结构/import 位置/API 处理方式），agent 只读它，不自由探索全仓库。（来源：ifnodoraemon + harishkumar）
17. **规则要剥掉会话只留原则**：从坏会话里总结规则时，把当次细节全删掉，只留普适原则——带会话细节的规则会过拟合，下次就不适用了。（来源：Grok Bot ANTIPATTERN #1）
18. **30 轮规则**：单会话超 ~30 轮，质量开始断崖。切新会话，只注入 SPEC/PLAN 级文档（~2000 token 恢复上下文）。
19. **频率是成本之王**：轮询默认一天 1-2 次；优先事件/webhook 而不是盲轮询；一次性验证逻辑编译成脚本/CLI，别让 agent 每次重写。（来源：Grok Bot ECONOMICS）
20. **"urgent" 是个坏词**：它会让 agent 跳过验证赶进度。定义一次 P0 政策（检查频率/打断条件），之后只说"按 P0 处理"。
21. **三可判据**：委托任何环节给 AI 前先问三句——需求**可描述**吗？结果**可验收**吗（机器可校验）？失败**可重复**吗？三者全"可"→ 放心委托；缺一项 → 人主导，AI 只提供灵感。（来源：hqwc《AI真能做游戏吗》09-20）

---

22. **一次视觉消费**：要看的图超过两三张时，**先把它们拼成一张带文件名标注的索引大图，再一次读完**。逐张送图既慢又贵，而且模型一次只能看到局部、没法横向比较。作者原话："一次视觉消费，顶一百次检索"。（来源：@op7418 Codex 实录 09-14；本工作区已用于 12 张立绘对照图，省下 11 次读图）
23. **端点重合**：做任何"循环播放"的东西——音频、视频、背景动画——让**首尾落在同一个点上**。音频把频率量化成循环长度的整数谐波；视频把首帧和尾帧用同一张图。循环处跳一下，九成是端点没重合，而不是素材不好。（来源：同上 Seedance 无缝循环做法 + 本作 `make_audio.py` 实测）

---

24. **指纹化验证"我到底在跟哪个模型说话"**：别信网关返回里的模型名——社区抓到过"便宜推理商静默把请求路由到更弱模型"。三步自检：① 问一个**该模型家族有明确、易查失败模式**的问题（特定 token 计数怪癖、冷门事实）；② 对比**延迟与输出风格**是否与厂商公布的数字吻合（突然变快或语气变了是信号）；③ 把**价格与该模型官方公布的成本**对比。（来源：r/LocalLLaMA 09-20；大陆环境下我们大多走第三方网关，这条适用性更高）

25. **写检查工具时，判据取"属性"不取"名单"，范围贴"真实路径"不贴"源码文本"**：这两句话各防一个方向。
    - **判据过窄**（`if name not in KNOWN_GOOD: fix()`）→ 把未知但正确的输入判成错误。本工作区实测样本：某第三方 Ren'Py MCP 的"中文字体自动配置"把我们合规的 OFL 字体换成了系统的商业字体（M-0008）。
    - **范围过宽**（"字符串里出现过就算"）→ 假警不断，**会喊狼来了的闸门迟早被无视**，比没有闸门更差（M-0009：把断言消息里的字、被覆盖掉的 screen 里的字都当成了会上屏的字）。
    - **收窄的正确方式是把真实路径的条件写进去**（哪些语句会渲染 / 逻辑行会跨物理行 / 哪份 screen 定义最终生效），而不是往白名单里加例外。
    （来源：第十一辑 renpy-mcp 实测 + 本工作区 `check_font_coverage.py` 两次误报）

---

26. **上下文瘦身：先机械删，再摘要；删掉的就让它删掉**：上下文管理的价值几乎全在**防溢出**，不在让模型变聪明。有效顺序是 **① 规则式硬删**（啰嗦的编译告警、重复堆栈、大段 grep 输出 —— 这些在进模型之前就该砍掉）→ **② 必要时才上模型摘要**。而"让模型用工具把删掉的片段捞回来"这种设计，**模型基本不用、准确率零提升、还多养一套状态机**。（来源：arXiv:2609.20804，176 组配对评测；本工作区对应做法 = 派活时只给 SPEC/PLAN 级文档，不给全仓源码）

27. **计划的分量按模型能力调**：给**弱模型**（本机 qwen3:8b 这类）计划是**必须的脚手架**，不给步骤它会卡在语法错误里；给**强模型**计划要缩成**显式停止条件**，否则它会在测试通过之后继续"投机式重写"。**换模型时要改的不是 prompt 的内容，是计划的分量。**（来源：同上）

28. **"做完"要能被外部独立复核，不能只由做的人自证**：`Harness-of-Harness` 五条机制里这一条最通用 —— **把"实现期的测试"和"独立评估"分开**。本工作区的形态是：实现放在游戏仓库里，验收放在 `tools/*.py` 与 `testcases.rpy` 里，**闸门不写在被测代码里**。（来源：arXiv:2609.01481，平均相对提升 52.25%）

29. **能力演示 ≠ 可借用外壳**：AI 让"做出来"变便宜了，**"能不能做"的价格没变**。凡是涉及照抄别人的美术 / UI / 地名 / 数值的项目，先过三问：**是不是非商业？有没有转换性表达？我是不是在替某个组织做宣传？** 三条里有一条不成立，就是下一个 72 小时下架的 Elderwood。（来源：第十二辑 Elderwood 事件，OpenAI 员工用 Codex 复刻 OSRS，Jagex 发 C&D 后三天关站）

---

30. **试玩只对"会读结果"的模型有用**：生成能力早就不缺，缺的是**把反馈看进去**这一步。同一张 benchmark 上，GPT-5.6 Terra 跑了 6 次试玩、**一次结果都没读**，于是照样交付了一个"攻击永远打不中"的版本；Claude Opus 5 看了 16 次里的 11 次，**从单帧里抓出 1 FPS 崩坏**。同一套工具、差了 3 倍分数和一百倍结果。**本工作区的对应物：`test` 跑完生成了 17 张截图，但没人看 —— 那和没跑是一样的。**（来源：第十四辑 #3 Ziva Full Game Playtest Agent 与它的六模型 benchmark）

31. **闸门保证的是"没坏"，不是"好"**：一个每道闸门都绿、每项功能都可达的版本，完全可能**没有任何值得一看的东西**。Astra 做的作品集游戏 2026-09-12 那版就是这样：六道阶段闸门全绿、22 件展品全部可达 —— 作者的原话是"**没有什么值得看的**"，真正的活是第二遍才做的。所以：**闸门用来拦住回归，不用来判定完成**；"完成"总得有人（或一次真的试玩）说一句"这个能看了"。（来源：第十四辑 #1《The Archipelago》复盘）

32. **能推导的，不要重打第二遍（parity rule）**：同一份数据在仓库里存两份，迟早会漂，而且**漂了没人知道**。本工作区实测：章节名原本在 `script.rpy` 写死一份、章节选择页又需要一份 —— 第 9 轮改成两张页面都从 `CHAPTERS` 表取，并加了一条断言**禁止剧本里再出现写死的章名**。判断标准很简单：**derived, not retyped**。同类：`ENDINGS` / `ACHIEVEMENTS` 表 → 收集册；`STORY_SCENES` → 成就判定。（来源：第十四辑 #1；本作第 9 轮落地）

---

33. **规则要能被测，不能只靠"写了就指望它生效"**：每条规则**要么配一个确定性检测器**（在 agent 自己的对话记录 / 产物里能判定的东西），**要么写明"为什么转录里没有东西能判定它"** —— 两者都没有的规则是**愿望**，不是规则。按这个口径给本项目 33 条房规做**第一次登记**（登记本身就是"这条规则在生效"的证据）：
    - **有确定性检测器**（可做成脚本 / grep）：#5（一个 diff 的关切数）、#13（附件是否存在：截图 / 前后数字）、#16（本文件行数 ≤200）、#19（轮询频率 ≤1-2 次/天）、#28（闸门文件不在被测代码目录里）、#32（剧本里不得再出现写死的章名）
    - **已内化成仓库里的闸门**：#13 → `testcases.rpy`（截图 + 断言链）、#16 → 本文件自身行数、#32 → `check_balance.py` 的断言
    - **判定不了、只能人判的**（如实登记，不假装能测）：#1 #2 #3 #4 #6 #7 #8 #9 #10 #11 #12 #14 #15 #17 #18 #20 #21 #22 #23 #24 #25 #26 #27 #29 #30 #31
    - **半可测的一条**：#14（对抗性验证）—— 它的检测器不是"转录里的某种模式"，而是"**有没有真的开一个新上下文**"。可以弱判（本会话有没有独立审计记录），但很弱；**重要的是把"弱"写出来，而不是假装它可测**。
    （来源：第十六辑 `JakeSelby/agent-harness`。它把这套做成了工具，并自曝**用这套工具抓出自己仓库里两个"上线了但其实什么都没做"的功能** —— 又一个"闸门自己也要有证据"的实证，对 M-0009 / 房规 #25）

---

34. **上下文是预算，不是仓库：只换入"当前动作需要的那一部分"**：四篇独立的上下文管理长文（掘金
    「三层压缩」/ CSDN「短暂 Agent 与持久化工程」/ 菜鸟「用 Plugin 管上下文」/ PEC Harness 工作坊）
    收敛到同一句话 —— **知识需要持续存在，不等于知识需要持续可见**。可执行的四条：
    · **加载要 JIT**：探索从便宜到贵（**Glob → Grep → Read**），别一上来读整仓；
    · **压缩从轻到重**：先 **microcompact**（只清老旧工具结果、不动对话结构）→ 再 **snip**
      （从头裁旧消息）→ 最后才 **auto-compact**（模型全局摘要）；
    · **阈值提前算**：`触发阈值 = 有效窗口 − 安全余量`（输出、工具结果、压缩本身都要占），
      **不要等 API 报错**；
    · **别把会变的东西放前缀**：时间戳 / 工具列表 / 当前文件放 System Prompt 开头 = **每轮缓存全失效**；
      静态的放前缀，动态的挪到用户消息附近。
    **反模式清单（可直接查）**：一次性读整仓 · 摘要只写"做了很多修改"（没有文件/决策/待办，
    下一批 agent 接不上）· 工具结果原样全量返回 · 每轮重排工具列表。
    （来源：第十七辑 §二；本工作区对应物 = `.workbuddy/memory/` 当日志 + `mistakes/` 当错误记忆，
    而不是把全部历史塞进一次对话）

35. **测"工具 A 还是 B"之前，先隔离你自己的个人配置**：一个**预注册**的 harness 消融实验
    （模型锁死 `claude-sonnet-5`，只换外壳 Claude Code vs Pi，4 任务 × 3 重复）第一版报出
    **7.0x~13.7x** 的 token 倍率，作者随后**自己推翻**：因为跑的是**没隔离的 `claude` 二进制**，
    本机 `~/.claude/` 里的 110 工具 / 10 插件 / 7 MCP / 68 个自定义 agent / 全局 `CLAUDE.md`
    **全漏进了每一次 trial**，把 Claude Code 的 token 抬高了 **39~52%**。改正后是 **4.2x~8.3x**。
    → **凡"A 工具 vs B 工具"的对比，先加一步隔离个人配置；否则量的是自己的机器，不是工具。**
    配套口径：同一单元格内 **Pi 的 CV≈0.2%，Claude Code 的 CV≈15~27%** —— 方差大的那一侧
    需要更多重复次数才敢下结论。（来源：第十七辑 §三 `nmlemus/harness-token-efficiency`）

36. **给"读输出的人"写：第一行就是动作，每轮重述状态，结尾一个具体下一步**：
    `ayghri/i-have-adhd`（**50,519★，一天涨 4,650★**）把"简洁"拆成了可检查的动作 ——
    ① **第一行是可执行动作**（不是背景、不是计划）；② 多步工作编号，每步一个动作；
    ③ 结尾给**一个两分钟内能做完的下一步**；④ 岔题留到最后单独问；⑤ **每轮重述状态**
    （"5 步里的第 3 步完成了"）；⑥ 时间给**具体单位**；⑦ **把完成的活显式写出来**；
    ⑧ 报错用陈述语气（不写"啊哦""好像有点问题"）。
    **例外必须写清**：**报告 / 索引类产出（要能当资料查）不套这 8 条** —— 会牺牲可检索性。
    判据：**这次产出是"要人立刻动手"，还是"要人以后回来查"？** 前者套 8 条，后者不套。
    （来源：第十七辑 §六）

37. **MCP 服务器"先量后开"：连上就付，不用也付**：本机实测（`_tools/harness_tax_probe.py`，
    2026-09-23）—— **7 个 MCP 服务器 / 95 个工具 = 10,519 token，占调用侧固定前缀的 50%**，
    其中 GitHub 一个就 **45 个工具 / 2,293 token**，而我们常用不到 10 个。
    · **启用前**：先量这个服务器的工具定义要占多少 token，再决定开不开；
    · **慢的时候**：先查"最贵的 5 个工具 Schema"（隐形成本通常藏在那儿）；
    · **CI 里**：防止工具膨胀（新增服务器要过预算）。
    完整账单：**调用侧固定前缀合计 20,900 token = 200k 窗口的 10.45%，每次请求都要重发一次**
    （MCP 50% / 记忆 37% / 技能 frontmatter 13%）。
    **这条是房规 #35 的量化版** —— #35 说"测工具前先隔离个人配置"，这条给出**隔离之后量到什么**：
    HarnessTax 论文说 Claude Code 首调固定块 27,011 token，**我们这边调用侧自己就贡献 20,900** →
    **tax 的大头确实在客户端那一侧**。（来源：第十八辑 §八 + `KaryawanSurga/mcp-context-budget`）

38. **记忆也要按 token 计预算，而且它比技能还贵**：同上实测 —— 常驻记忆文件 **7,721 token（37%）**，
    其中**工作区 MEMORY.md 4,733** + 用户级 MEMORY.md 1,540 + USER/SOUL/IDENTITY 1,448。
    · **工作区 MEMORY.md 是最该瘦身的一份**（它增长最快、又整份常驻）；
    · 判据：**超过 ~3,000 token 就蒸馏**（按主题合并到长期笔记，删掉已完成的当日细节）；
    · 复核手段：`_tools/harness_tax_probe.py`（零依赖，可重跑；无 tiktoken 时退回近似并**标注口径**）。
    （来源：第十八辑 §八）

39. **交接文档必须分清"已决定"和"待确认"**：中文社区（worldprogramming 转述）+ 掘金同题 + CSDN 三篇
    收敛到同一套操作，我们只取**不与我们已有三层（mistakes/ + 每日日志 + 自动化记忆）重复**的那部分：
    · **区分两栏**：已确认的目标/约束/方案/文件/验收标准/待办 **vs 单独标出的未决问题**
      —— 否则接手的一方会把讨论里的猜测当定论；
    · **One-shot 不是信任问题是算术**：生成错了**别就地打补丁**，改写 prompt + 回滚重来。
      十轮小修小补的代价会在两小时后以"模型开始自相矛盾"的形式**一次性结算**；
    · **计划落文件，不要留在对话里**：同会话里"计划的 token 在实现时已经用掉了"，
      模型只能从自己压缩过的内部表示里重建意图；
    · **50% 交接**（激进者 15%）：到点就写交接、开新会话。反方说"完整会话连贯性更好" —— 但
      **交接的损失可预期可管理，长上下文退化的损失隐蔽且不可预期**。
    **不采纳**：完整 handoff 层（与已有三层重复）。（来源：第十八辑 §二.4）

---

*维护：本文件超 200 行时先砍最通用的条款（那些已内化为默认行为），而不是加新条款。*
*最后更新：2026-09-23（第十八辑：+37 MCP 先量后开 / +38 记忆按 token 计预算 / +39 交接分清已决定与待确认，共 39 条）*

