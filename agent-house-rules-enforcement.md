# Agent 房规 · 执行登记表（派生文件，不要手改）

> ⚠️ **本文件由 `_tools/house_rules_enforcement.py` 生成** —— 手改会在下次重跑时被覆盖。
> 重跑：`python _tools/house_rules_enforcement.py`

> 生成时间：`2026-09-24 17:36`（**脚本打戳**，不手写时间）

> 依据：房规 **#33**（元规则）—— *每条规则要么配一个确定性检测器，要么写明「为什么判定不了」；两者都没有的规则是**愿望**，不是规则。*

> 主件 `agent-house-rules.md` **28 条 / 259 行**；撤下条款在 `agent-house-rules-archive.md`。


## 一、总览

| 档 | 条数 | 含义 |
|---|---|---|
| ✅ **已接线** | **7** | 有确定性检测器（脚本可跑，`--check` 会校验它还在） |
| 👤 **只能人判** | **21** | 如实登记，**不假装可测**（#33 明确允许这一档存在） |

> ⚠️ **这两个数不是「好/坏」评分。** 房规里有一类是**故意**不机械化的 ——例如 #15「发布 / 权限 / 资金类动作 = 人工闸门」，它的正确形态就是「永远由人按」（房规 #50）。把它们算作「没接线」是**误读**：那一档存在的意义是**写清边界**，不是刷高自动化率。


## 二、逐条登记

| # | 条款 | 档 | 检测器 / 位置 |
|---|---|---|---|
| 1 | 先复现，后修复 | 👤 人判 | — |
| 11 | 两击规则 | 👤 人判 | — |
| 13 | 每次附证明 | 👤 人判 | — |
| 14 | 对抗性验证 | 👤 人判 | — |
| 15 | 发布/权限/资金类动作 = 人工闸门 | 👤 人判 | — |
| 16 | 上下文预算 | ✅ 已接线 | `_tools/skill_budget.py` · `_tools/house_rules_enforcement.py` |
| 22 | 一次视觉消费 | 👤 人判 | — |
| 23 | 端点重合 | 👤 人判 | — |
| 24 | 指纹化验证"我到底在跟哪个模型说话" | 👤 人判 | — |
| 25 | 写检查工具时，判据取"属性"不取"名单"，范围贴"真实路径"不贴"源码文本" | ✅ 已接线 | `_tools/gate_recall_probe.py` |
| 27 | 计划的分量按模型能力调 | 👤 人判 | — |
| 28 | "做完"要能被外部独立复核，不能只由做的人自证 | 👤 人判 | — |
| 29 | 能力演示 ≠ 可借用外壳 | 👤 人判 | — |
| 31 | 闸门保证的是"没坏"，不是"好" | 👤 人判 | — |
| 32 | 能推导的，不要重打第二遍（parity rule） | 👤 人判 | — |
| 33 | 规则要能被测，不能只靠"写了就指望它生效" | 👤 人判 | — |
| 35 | 测"工具 A 还是 B"之前，先隔离你自己的个人配置 | 👤 人判 | — |
| 40 | 压缩「工具输出」是纯赚，压缩「模型怎么说话」要赔 —— 这两件事不许混着做 | 👤 人判 | — |
| 42 | 开工前先看有没有人在写；有就撤，不要抢 | ✅ 已接线 | `round-research-index` |
| 43 | 压缩次数是独立维度；检查点不覆盖 Bash | 👤 人判 | — |
| 44 | 子智能体输出是"带出处的工具结果"，不是继承父级权威的指令；AI 会用废弃 API | 👤 人判 | — |
| 45 | 提议给模型，裁决给代码；记忆要能被查，也要能被审 | ✅ 已接线 | `_tools/mistakes_seal.py` |
| 46 | 外部内容不得直写记忆；记忆要有版本，也要能被审 | ✅ 已接线 | `_tools/memory_health.py` |
| 47 | 长任务的产出要"边做边落盘" | 👤 人判 | — |
| 48 | 克隆来的仓库先只读审配置，再决定信不信；绝不开启"自动信任全部项目 MCP" | 👤 人判 | — |
| 49 | 记忆条目的回归测试必须指向仍存在的路径；项目终止/归档时同步迁移或标 `stale` | ✅ 已接线 | `_tools/memory_health.py` · `_tools/doc_ref_check.py` |
| 50 | 能机械化的一律机械化，判断力留给人 —— 分界线要写明 | ✅ 已接线 | `_tools/mistakes_seal.py` · `_tools/known_limits_check.py` |
| 51 | 把「我们哪里不行」做成门禁，而不是写在前言里 | 👤 人判 | — |

### 已接线明细

- **#16** ← `_tools/skill_budget.py`（GATE）：量**技能** ≤500 行 / ≤5000 token
- **#16** ← `_tools/house_rules_enforcement.py`（GATE）：量**房规自己**的行数 vs 200 行上限（原来没人量它 → §三 的实测失效项）
- **#25** ← `_tools/gate_recall_probe.py`（GATE）：闸门自身的召回 + 负对照（守「判据取属性不取白名单 / 范围贴真实路径」）
- **#42** ← `round-research-index`（SKILL）：skill §0「开工三步」= 这条的操作化（git status + mtime + 陌生脚本）
- **#45** ← `_tools/mistakes_seal.py`（GATE）：`seal/check` 把「记忆要有版本、要能被审」变成可跑的不可变基线
- **#46** ← `_tools/memory_health.py`（GATE）：R5 抗污染（适用范围声明）
- **#49** ← `_tools/memory_health.py`（GATE）：R7 回归测试路径存在性 → 分类计数
- **#49** ← `_tools/doc_ref_check.py`（GATE）：C3 文件路径 + C2 条款编号（指针失效检测）
- **#50** ← `_tools/mistakes_seal.py`（GATE）：DSH §13 的「27 道机械检查」思路：能机械化的落成 seal/check
- **#50** ← `_tools/known_limits_check.py`（GATE）：「哪里不行」做成门禁

## 三、已实测失效的（有证据，不是推测）

> 这一节的数是**每次跑时量出来的**，不是手写（手写的会烂，M-0024）。

- **#33** —— 元规则的检测器就是本文件；在第二十二辑被误砍进归档后**整整两轮无人发现**，直到用户追问「砍了之后房规还如何执行」。修法：搬回主件 + 归档纪律加「元规则不许砍」
- **#16** —— `agent-house-rules.md` 实测 **259 行** > 自定上限 **200 行** → **该砍了**（⚠️ 先归档再砍）。本文件现在会量它，所以 #16 从「没人量」升级为「被量、且已超」——但**只报告不阻塞**（M-0009：一次性全阻塞会让闸门永久红灯）

> 实测值：`agent-house-rules.md` = **259 行** / 上限 200 行。


## 四、扫描到的引用热点（**仅参考**）

> 下面是「还有哪些产物提到过这个编号」。⚠️ **提到 ≠ 接线**：实测绝大多数是**注释 / 正文里的引证**（如「这条是房规 #35 的量化版」），不是检测器。所以本表**不用它判定「已接线」**，只用来找「哪条规则其实经常被用到、却没有检测器」。

> 只统计**主件里真实存在**的编号；扫到 1 个不存在的编号已丢弃：#9876。

- #13（👤人判）：出现在 错误记忆
- #25（✅已接线）：出现在 闸门脚本
- #28（👤人判）：出现在 错误记忆
- #32（👤人判）：出现在 闸门脚本
- #33（👤人判）：出现在 闸门脚本
- #35（👤人判）：出现在 错误记忆/闸门脚本
- #42（✅已接线）：出现在 闸门脚本
- #45（✅已接线）：出现在 错误记忆
- #49（✅已接线）：出现在 skill/闸门脚本
- #50（✅已接线）：出现在 闸门脚本

> 注：**#33 的检测器就是本文件自己**（自指）—— 它不在「已接线」档，因为「这条规则该不该机械化」本身只能人判；本文件只把**登记动作**机械化了。

