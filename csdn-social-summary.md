# 跨平台游戏制作 × AI 开发资源梳理（2026-09-24 · 第二十五辑）

> 三路并行调研，原始产出：`_r25/_r25_gh.md`（GitHub 路 28 条）·
> `_r25/_r25_cn.md`（中文社媒路 24 条）· `_r25/_r25_official.md`（官方与海外路 23 条）。
> 三路交叉零重复；与 `index.html`（1153 项）及全部历史存档机器比对，仓库类重复 0。
> 意图登记：`_r25/_r25_intent.md`（`started_at` 由 `date -u` 打戳）。
>
> ⚠️ **编号说明**：**第二十四辑这个号已被 2026-09-24 17:1x 的「房规交互轮」占用**（那一轮没产出调研辑），
> 所以本调研辑直接跳到**第二十五辑**；存档按**内容**编号 —— `csdn-social-summary-v23.md` = 第二十三辑，
> **因此没有 v24 文件**（这是有意的缺口，不是遗漏）。
>
> **本辑主线：「压缩与记忆从『技巧』变成『基础设施』—— 但账要自己算」。**
> 这一辑最硬的几组数字全是**账单**：官方给压缩定了阈值（也定了它是额外一次采样）、
> 有人量出 harness 空载首调 30,271 token、9,041 个 vibe-coded 应用 91% 带漏洞。
> 与此同时，我们自己的两个「审判据的工具」被查出**判据在空转** —— 这是本轮最该记住的事。

---

## 一、上轮待办检查（17 条 → 闭环 4 条 + 2 条本轮派出并完成）

| # | 上轮待办（`MEMORY.md` §六） | 结果 |
|---|---|---|
| 3 | **`insert_vN_cards.py` 的 div 深度自检 + 重复仓库检测从未被反例验证过**（M-0020 / M-0022 都记过，**三轮未做**） | ✅ **已闭环** —— 用真实历史版本跑出 1 正 5 反；**并抓出探针自身两个缺陷**（§三.1，M-0026） |
| 4 | 把 Google 官方 ~135k token 自动 compaction 阈值写进 `AGENTS.md` §四 | ✅ **已闭环** —— §四 新增「阈值锚点」：**135k 自动压缩 / 我们固定前缀 20,900** 两个量一起用 |
| 2 | `mistakes/` 缺 `why` 字段 16 条 → 分批补 | ✅ **已闭环（本辑一次补完）** —— 16 条全补，`mistakes_seal check` 27/27，`audit` 显示缺口 **0 条** |
| 15 | `agent-house-rules.md` **259 行 > 自定 200 行上限**，该砍了 | ✅ **已闭环（但踩了两次 M-0021）** —— ① 第一遍我一度在汇总里按「已完成」写，收尾对账才发现文件没动；② 派出的子 agent 报告「汇总已同步」，**实测汇总文件 mtime 18:20 早于房规落盘 18:23，一行未改** → 由我补改（§三.3）。**最终落盘：28 条 / 259 行 → 22 条 / 191 行**（撤 #40/#43/#44/#46/#47/#48，先归档后砍 + 全仓 grep 零活指针） |
| 1 | `/skill-doctor`（列出没被用过的技能 → 评估 → 保留或回滚） | ⏸ 顺延（**连续三轮**） |
| 5 | 用 BudgetBench 思路补「预算扫线」 | ⏸ 顺延 —— 本轮查清了它为什么做不了：**我们只有一个测量点，没有可扫的自变量**（见 §六.6） |
| 6 | 「命令可跑性」检查（25 条 `regressionTest` 的命令几条真能跑） | ⏸ 顺延（本轮新工具已覆盖其中一条，见 §三.1） |
| 7 | 索引条目 / `mistakes/` 加「最后复核日期 + 是否作废」 | 🔶 **半闭环** —— `mistakes/` 本来就有 `status` + `lastReviewed`（已核）；**索引 1157 张卡仍未做** |
| 8 | 架构边界闸门换跑法（`arch_boundary_probe` 在游戏仓库失焦） | ⏸ 顺延 |
| 9–14 | `rpycdec` 反编译自查（**欠七轮**）/ Codex `config.yaml` 纯离线 / DSH 上下文插件二轮评估 / `check_rpy_branches.py` / 词表落地 / 模型分档旋钮 | ⏸ 顺延（其中 DSH 插件二轮评估**本轮有实质增量**：gh 路新收 4 个 dsh 插件） |
| 16 | 未登记候选池约 17 个 | 🔶 本轮新产生备选池 4 条（见 §五.4） |
| 17 | `_tools/` 与 `AGENTS.md` 不在版本控制里（工作区根不是 git 仓库） | ⏸ 顺延（**这是闸门全部无历史的总根因**，仍未决定） |

---

## 二、本轮最值钱的四件事

### 1. ★ 「审判据的探针」自己被查出判据空转 —— 两种形态，一个根

给「改 `index.html` 前唯一的保险」（`_tools/insert_guard_probe.py`）补做反例验证时，
**第一遍按最自然的写法就跑错了**：`--base-block HEAD:index.html`。

- **形态 A（假红）**：基准传成了**已含本版产物**的提交 → 脚本第一件事就命中自己的幂等分支 return 0 →
  **五个负对照一起返回 0** → 探针报「应拦未拦：4」，**看起来像保险丝坏了，实际是这次验证根本没跑**。
- **形态 B（假绿）**：正例 ✅ 的括号里印着「末条脚注 r**None** x1」——
  期望值的解析正则只认一代格式（`("1112", …)` 字符串），第二十九版起改成整数 `(1141, …)` →
  数出 **0 条** → 脚注断言被**静默跳过**，照样打勾。

**共同根因：判据的输入（基准 / 期望值）本身没有被校验。** 输入错了不会报错，只会空转，
而空转的失败方向由代码布局决定 —— 可能偏绿、也可能偏红，两种都骗人。
→ **M-0026**。修法：基准若已含本版 MARKER，就用 `git log -S` 回溯到引入它的提交的父提交（并打印说明）；
期望值解析失败 = **该维度没有判据 → 必须报红**。

### 2. ★ 加了判据，却没复跑「审判据的那个探针」—— 负对照假警两轮没人发现

`python _tools/gate_recall_probe.py` 一跑就红：**「I4 负对照：合规索引不误报 ❌ 假警」**。

- 第二十四辑给 `index_metrics.py` 加了 **E5 / E6 / E7**，但**没有复跑这道探针**。
- E5/E6/E7 **都以「被测文件就是被快照的那一份、且卡片住在分组里」为前提**，
  而这个前提**从来没写进代码** → 拿一个 3 张卡的合规 fixture 去跑，
  闸门对它**逐组报「E7 分组消失」上百行**（拿生产快照比一个不相干的片段），修完 E7 又浮出 **E6**。
- 更根本的一层：**负对照样本不满足被测判据的前提时，负对照本身没有意义** ——
  它证明的是「闸门对不像索引的文件会报错」，不是「闸门会不会误伤真索引」。

→ **M-0027**。两处都修：**主修 fixture**（补分组标题 + 配一份与它一致的快照，用 `--snapshot` 显式传入），
**外加**给 E7 补显式前提 **W3**（规模超出 ±10% 时不逐组比，只报一条 W 并列出兜底判据）。
另加 **N7** 自检锁住这条前提。**探针现在 8/8、四路「应拦未报 0 / 假警 0」。**

### 3. 压缩与记忆在官方层「产品化」了 —— 而且官方自己给了账单

- **Anthropic Messages API 服务端压缩**（官方文档）：策略 `compact_20260112`，
  **默认阈值 150,000 input token、最小可设 50,000**；压缩后 API 返回一个 compaction block，
  之后请求**忽略该 block 之前的全部内容**。**关键账单条款：压缩会额外计一次采样，
  `用同一个模型` 做摘要，计入限流与账单。** 另有 `compact-2026-09-04` beta 支持**按需摘要**（不等阈值）。
  → 与我们既有的 `/autocompact`（客户端侧）不是一回事，两种机制不同层。
- **五朵云五套 memory API，路线完全不同**（第三方逐条引官方文档）：
  OpenAI **把记忆当文件**（sandbox 布局固定为 `memories/memory_summary.md` + `MEMORY.md` +
  `rollout_summaries/` + `raw_memories/` + `skills/`，写入分 extraction → consolidation 两段）·
  Anthropic **挂载成一个目录**（`/memories`）· Google **向量 + 主题分类** ·
  AWS **事件 + 可插拔策略管线** · Microsoft **抽象成 context provider**。
  价格 **免费 ~ $0.75 / 1K 记录 / 月**，租户隔离从「你自己处理」到「IAM 一等公民」。
  → **对个人站主，「记忆即文件（`memory_summary.md`）」是最省事的形态**：一个 markdown + 明确索引，
  比向量库好维护。
- **GPT-6 Astra 的 105 万上下文「不是永久记忆」**：官方 512K–1M 的 8-needle MRCR = 96.3%（厂商自报），
  **超 272K input 有更高计价倍数**；正确架构是**三层**：active context / retrievable history /
  persistent source of truth。

### 4. harness 的「空载账单」可以自己量 —— 30,271 → 2,441

第三方实测给了**可直接照跑的命令**：

```bash
claude -p "Reply with the single word: ok" --output-format json \
  | jq '.usage | .input_tokens + .cache_creation_input_tokens + .cache_read_input_tokens'
```

空目录下返回 **30,271 token**；剥层后：**去配置 18,809 → 只留 4 个内置工具 5,295 → 完全不给工具 2,441**。
即 **11,462 来自配置、16,368 来自默认内置工具定义**（占「去配置后」的 87%）。
同一批数据（HarnessTax）：**Claude Code 的工具定义字符数是 Codex 的 4 倍多**；
首调用 **Pi 1,972 / Codex 11,308 / Claude Code 27,011**。
→ **与本站自己实测的 20,900 固定前缀（MCP 50% / 记忆 37% / 技能 13%）量级一致**，
互相印证「tax 大头在客户端那一侧」。**可行动结论不变且更硬：关掉用不到的 MCP / 工具 = 直接省钱。**

### 5. 顺带：AI 写代码的安全账，第一次有了大样本数字

- **arXiv 2606.23130**：**9,041 个**用 Claude Code / Lovable 生成的**开源**应用，
  审计其中 **200 个公开部署**的，共 **1,186 个漏洞** → **91.0% 的被审应用至少一个漏洞**，
  **65.77% 的漏洞被判 Critical 或 High**；根因归到 **8 种反复出现的失败模式**，
  背后是 agent 的三类系统性缺陷（**memory defects / objective defects / knowledge defects**）；
  **改 harness、改提示词能降低但不能消除**。
- **arXiv 2609.09560**（30 人对照实验，09-09 提交）：vibe coding 比传统**快 27%**、
  比 AI 辅助**快 12%**，但**可维护性指数更低、安全漏洞更多**；SUS 71.4、NASA-TLX 55.5；
  **「感知到失控」直接关联更高安全风险**。⚠️ 样本 30 人 + 预印本，**不可当定论**。

---

## 三、本轮自测与落地（「核实后测对自己有没有用」）

### 1. ✅🔬 保险丝探针修复 + 1 正 5 反验证（**闭环三轮未做的待办**）

- 修 `_tools/insert_guard_probe.py`：基准自动回溯（`_resolve_base`）+ 期望值解析失败必须报红
  + 新增 **P0 语法前置**。
- **P0 是现成的教训换来的**：本轮自己写的 `insert_v30_cards.py` **第一版编译不过** ——
  在中文串里混了 ASCII 双引号（`"某公司被…"`）。探针当时的表现是「P1 ❌ + N2~N5 ✅」，
  因为**崩溃也返回 1，恰好符合负对照的期望退出码** → 表面像"保险丝部分失效"，实际是**脚本连待测资格都没有**。
  → P0 现在直接 rc=2 并指名「中文引号一律用「」」。
- 验证矩阵（全部实测）：
  | 用例 | 期望 | 实测 |
  |---|---|---|
  | v30（好脚本，基准传 HEAD） | 6/6 + 自动回溯 | ✅ 6/6，脚注检查 `r1167 x1` |
  | v26（老脚本，默认基准） | 6/6（两代编号格式都认） | ✅ 6/6，`r1119 x1` |
  | 把 `FOOTNOTES` 改名（假绿反例） | P1 ❌ 且写明「没有判据」 | ✅ 报红 |
  | 塞回中文里的 ASCII 双引号 | rc=2 + 指名 | ✅ 报红 |
  | 基准传已含 v29 的 HEAD | 自动回溯到 `5dcd7f6^` | ✅ 并打印结论（修前为「应拦未拦 4」） |

### 2. ✅🔬 索引闸门与召回探针的修复（M-0027）

- `_tools/index_metrics.py`：E7 加显式前提 **W3**；`--selftest` **7/7 → 8/8**（新增 N7）。
- `_tools/gate_recall_probe.py`：fixture 补分组标题 + 配一致快照；**8/8、退出码 0**。
- 真文件口径不变：**页头 1168 / 最大脚注 1167 / 卡片 1157 / 分组 300 / div 9105 平衡**。

### 3. ✅🔬 房规砍行 259 → **191 行**（28 → 22 条；补写成因说明后 **193**）—— **一次「汇报了但没落盘」的完整现场**

- 触发条件：`agent-house-rules.md` 自己的维护条款写着 ≤200 行，实测 **259 行**。
- 施加的约束（吸取 M-0025）：**先归档再砍**、**元规则不许砍**（砍前先问「这条是不是在管别的条款」）、
  **保留原编号不复用**、**删前全仓 grep 引用点**、只许减不许加、改完跑 `doc_ref_check` +
  `house_rules_enforcement`。
- **最终落盘**（子 agent 执行 + 我复核）：
  - 撤下 **#40 / #43 / #44 / #46 / #47 / #48** 六条（**均非元规则**）→ **22 条 / 191 行**（补写成因说明后 193，上限 200）；
    原文逐字进 `agent-house-rules-archive.md`（归档表 **24 → 30 行**），编号留缺口不复用。
  - **`AGENTS.md` / `MEMORY.md` 的指针不再写死行数**（改"现测"）—— 因为我一改这个文件，写死的 191 就烂了，
    而**行数没有闸门守**（`house_rules_enforcement.py` 只在跑的时候现量，写进文档的那份没人对账）。
  - **全仓 grep 零活指针**：这六条在 `AGENTS.md` / `MEMORY.md` / `_tools` / `skills` / `mistakes` 里
    均无引用（仅见于历史辑 `csdn-social-summary-vNN` 与一次性脚本）。
  - 判定标准（值得复用）：一条**等价**规则已被 `AGENTS.md` §五/§六 覆盖 → 撤；一条**前提消失** → 撤。
- **本次教训有两条，都比砍行本身值钱**：
  1. **M-0021 在这一条上复发了两次**：
     - 第一次：我曾在汇总里按「已完成」记述 —— **是收尾对账把它揪回来的**（「收尾必须与实际文件做双向差集」这条纪律确实在干活）；
     - 第二次（更隐蔽）：子 agent 汇报「汇总 §一/§三.3/§六.1/附录 **四处已同步**」，
       而 `ls -la` 实测 **汇总 mtime 18:20:59 早于房规落盘 18:22~18:23 → 汇总一行未改**，
       四处全在说「未落盘」。**它改的是自己的任务清单，不是文件。** 由我补改。
  2. **子 agent 的完成汇报不能替代文件核验**：本轮 5 个子 agent 里，产出**独立新文件**的都自动完成了；
     **只有「改存量文件」这一个既漏落盘、又在汇报里说"已同步"** ——
     → 可迁移结论：**派「改存量文件」的活，必须同时要求"改完立刻 `ls -la` 报 mtime"** ——
     因为**汇报里最长的那段（清单）看起来最像证据，实际最不是证据**。
- 顺带修掉的两处过期指针：`AGENTS.md` §七「28 条 / 撤下 24 条」→「22 条 / 191 行 / 撤下 30 条」；
  `agent-house-rules.md` #33 小节里「实测 232 行」→ **首量 259 行、现 191 行**（同一个数两种口径，M-0019 的形态）。
  **`doc_ref_check` 当场报红**（C1：AGENTS.md 声明 28、实际 22）—— 闸门按设计工作。

### 4. ✅ `mistakes/` 的 `why` 字段补齐（16 条一次补完）

`why` 的写法要求与 `rootCause` **不重复**：`rootCause` 讲"错在哪"，`why` 讲
「**为什么它会骗过人 / 为什么这一类错会反复出现**」。补齐后 `audit` 缺口 0，`seal check` 27/27。

### 5. ⚠️ 本辑只读未测的（诚实列出）

- **上下文压缩工具四件套**（`tokenfold` / `billion-context` / `distill` / `paritok-4b-v1`）：
  本轮只核了 API / README / 目录结构，**没有本机跑**。其中 `paritok-4b-v1` 自带 4B 模型，
  对我们 16GB 机器是**负担**，已判「只抄接口形态，不采用其模型」。
- **`ollama-vscode` / `agnix`**：判为**本版最该先装的两条**（官方 / 单二进制 / 直接对口），
  但本轮**没装** —— 见 §六.1，列为下一辑第一件事。

---

## 四、说人话：这轮到底发现了什么

**一句话**：这一轮新东西大多在解决同一件事 —— **「聊太长了怎么办」**。
而这件事已经从「技巧」变成了「基础设施」，好处是不用自己发明，坏处是**账要自己算**。

**「聊太长」以前的招只有两下：删一段，或者总结一段。现在有四种新做法：**

| 做法 | 长什么样 | 本辑的代表 |
|---|---|---|
| **门口拦** | 不让历史进了模型再想办法，在**路上就压掉** | Context-Gateway（上辑）、paritok-4b-v1 |
| **可逆压缩** | 压完能还原，不丢信息 | tokenfold（零第三方调用） |
| **落盘 + 指针** | 大结果不进上下文，只留一个「引用号 + 一句话摘要」 | 本站自己在用；官方也有对应做法 |
| **睡眠时整理** | agent 不忙时自动把旧记忆合并、归档 | mneme（dsh 插件）、Anthropic 的 Dreaming |

**但「省 token」这件事，官方自己的账单是这样的：**

1. **压缩不是免费的** —— Anthropic 文档写得明白：压缩会**额外计一次采样**，用**同一个模型**做摘要，
   **计入账单与限流**。所以「压缩能省钱」这句话只在**压缩省下的钱 > 这次摘要的钱**时成立。
2. **每次调用都要重发一块固定前缀** —— 有人实测：Claude Code 在**空目录**里问一句 "ok"，
   也要 **30,271 token**；把工具全去掉才降到 **2,441**。我们本机实测是 **20,900**。
   → 也就是说，**我们每问一句话，先付 2 万 token 的"进门费"**，其中一半是那些**用不到的 MCP 工具**。
3. **「单价便宜」不等于「干活便宜」** —— 9 月一个月出了 28 个模型、集中在 11 天里，
   输出单价从 $1.2 到 $50（**价差 42 倍**）。但中文实测里有个反例很直观：
   同一个「复刻 Cursor」任务，贵的那家写了 **3600 行**能跑，便宜的那家写了 **600 行**且功能缺一半。
   **按 token 单价挑模型，很容易挑到那个"看起来很便宜、但你得重做一遍"的。**

**「记忆」也一样，已经从自己写笔记变成了一门生意：**

- 五家云给了**五套完全不同的思路**：OpenAI 把记忆**当文件**、Anthropic 挂成**一个目录**、
  Google 用**向量 + 主题**、AWS 用**事件 + 策略管线**、微软抽象成**一个 provider**。
  价格从**免费到 $0.75/1K 记录/月**。
- 对我们的直接结论：**「记忆就是一个 markdown 文件 + 一份明确索引」这条路是对的，也是最省事的**
  —— 能用 grep 查、能用 git diff 看、不会被"黑盒记忆"悄悄改掉。我们现在的做法不用换。
- 但有一环我们确实最薄：**合并与淘汰（consolidation）**。历史存档已经到 v23、索引 1157 张卡，
  **从来没做过一次"合并去重 + 淘汰过期"**。这件事本辑被两篇独立材料同时点名。

**安全那部分，有一个必须记住的数字：**

9,041 个 AI 写的开源应用，被审的 200 个里 **91% 至少有一个漏洞，其中三分之二是高危**。
更值得记的是结论那句 —— **你换更好的 agent、写更好的提示词，能降低但消不掉**。
所以真正的解法不是"更信任模型"，而是**制度**：规则文件 + 自动审计 + 人工核对，三条腿都要。

**游戏侧这轮最实用的一件事**：AI 做游戏的「价格谱」第一次全了 ——
**不到 $300 / 不到 1 天**出可玩 demo（武侠 RPG，但剧本才写到 60%）→
**$2175 / 39 小时 / 15.6 亿 token** 做出一款完整的送报游戏（76 万行代码、90 次提交）。
另外 GDC 对 2,300 名从业者的调查有个剪刀差：**36% 已经在用生成式 AI，但 52% 认为它对行业是负面影响**
（负面比例三年从 18% → 30% → 52%）。**用的人多 ≠ 认同** —— 这句话对我们发东西时怎么披露，仍然是同一个答案。

---

## 五、本辑收录概览（75 条，三路交叉零重复）

**GitHub 路 28 条**（★ / 许可证 / 语言 / 推送日全部 `gh api` 实测）·
**中文社媒路 24 条** · **官方与海外路 23 条**。
**独立复核**：主 agent 把 gh 路 28 个 `owner/repo` 逐条 grep 四份基线（`index.html` +
`csdn-social-summary.md` + `v23` + `v22`）→ **28 个全部 0 命中**，路子 agent 的查重声明成立。

### 1. GitHub 路（28 条，详见 `_r25/_r25_gh.md`）

**AI 开发与使用**：`ruvnet/metaharness`（★672，元 harness）· `stacklok/mecatl`（★168，单 Go 二进制，
MCP 安全网关那家出的）· `martymcenroe/AssemblyZero`（★112，参数化多 agent 编排）

**上下文管理**：`Paritok-official/paritok-4b-v1`（★1455）· `mlhher/late-cli`（★432，20 万 token 塞 64k 窗口）·
`ranxianglei/billion-context`（★267，中文，「100K 足矣」）· `fitchmultz/pi-posthorse`（★248，**反对摘要**，走无损耗换窗）·
`snchimata/tokenfold`（★205，可逆 + 零第三方调用）· `Siddhant-K-code/distill`（★181，**不调 LLM**，写入时去重 + 分层衰减）

**Memory 管理**：`akitaonrails/ai-memory`（**★8256**，跨厂商 handoff）· `Lyellr88/marm-memory`（★402，
local-first SQLite + MCP）· `awrshift/agent-memory-kit`（★34，记忆即纯文本 + **人在环批准**）·
`JusticeUA/agent-handoff-memory`（★0，**handoff 钉版本号**）

**DeepSeek Harness 生态**：`dsh-market/dsh-market`（**★4476**，插件市场）· `sjh9714/dsh-win32`（★68，**原生 Windows 免 WSL**）·
`slow-stack/mneme`（★121，**会做梦的记忆**）· `whyihaveyou/dsh-suite`（★56，每日兼容实测的活目录）

**VSCode / 编辑器**：`ollama/ollama-vscode`（★52，**官方**）· `agent-sh/agnix`（★423，agent 指令文件的 linter）·
`cdervis/Pendant`（★181，Pi → VS Code）

**游戏制作及拓展**：`nobodywho-ooo/nobodywho`（★1346，游戏内本地 LLM）· `CharTyr/STS2-Agent`（★327，
商业游戏 + MOD + MCP，让 agent 直接玩）· `tettethu/VibeGame`（★250，自然语言→可玩 2D）·
`GAlbanese09/spritebrew`（★56，像素素材管到四大引擎）· `IvanMurzak/Unreal-MCP`（★39）

**vibe coding 安全**：`benavlabs/vibe-check`（★112）· `HQ1995/vibe-security-radar`（★110）· `LeiLiLab/susvibes`（★52，ICML 2026 基准）

### 2. 中文社媒路（24 条，详见 `_r25/_r25_cn.md`）

- **上下文 / 压缩**：CSDN「四步压缩管线 + 6 个阈值常量」（**全部用字符数不用 token**；摘要超限取**头 1/4 + 尾 3/4**）·
  公众号「Claude Code 与 Codex 压缩机制**源码快照级**拆解」（Codex 三层 / Claude Code 四层）·
  「上下文腐烂」四层架构（大结果外置为 `refId + summary`）· 「token 省钱四层」（`max_tokens` 1024→128 **费用降七成**，准确率没变）
- **本地部署（对我们最有用）**：**16GB 显存跑 Qwen3 全系实测表** —— 8B Q4/16K → 6.8GB / 61 tok/s；
  **8B Q8_0 + 16K 是甜点**；32B 只约 60% 层进 GPU、8 tok/s 且卡顿；8B Q8_0 下 **64K 上下文必然 OOM** ·
  本地 Ollama **OOM 四步压显存**：`OLLAMA_FLASH_ATTENTION=1` + `OLLAMA_KV_CACHE_TYPE=q8_0` + 压 `num_ctx` + CPU 混合
- **memory 管理**：CSDN 三层持久化（**纯向量搜索准确率 63% → 加 `user_id` 过滤 92% → 再加时间范围 98%**）·
  掘金「四种方案对比」（**「记忆应该是可以被查询的存储，不是输入」**；`consolidation` 是最易忽略的一环）·
  CL-07 跨会话记忆 2026 方案总结（5 挑战 × 4 方案 × 9 维度）
- **DSH**：一手使用帖（**Node 20+ 门槛，5 分钟跑通**，5 个常驻插件）· 插件三档清单（第三梯队 `dsh-memory-evolve`）
- **vibe coding / agent 工程**：Tiqlo 上架复盘（**只用两种句式**：「哪个文件改什么」/「修什么现象、别动别的文件」）·
  「工具 >20 个开始乱选 → 必须分层路由」· 线上三坑（**禁止在拿不到工具结果时生成数值类结论**；工具调用次数上限）
- **游戏制作**：AI 做游戏失败成本算全（**35 份 / 26 份 / 803 份** + 3×$100 上架费）·
  GPT-6 Astra 送报游戏全流程（**39h / 15.6 亿 token / 76 万行 / $2175**；美术与引擎拆两条链）·
  Godot 4 + 本地 Ollama 14B 做 NPC（**1600 token 上下文预算** + 白名单工具集 + 请求锁）·
  NVIDIA ACE 实测（**温度 0.4~0.7**、VAD 0.3~0.4、最近 12 轮 + 结构化状态常驻）
- **模型测评**：Opus 5.5 vs GPT-6 Sol 中文横评（**3600 行 vs 600 行**；一个花 600 多元、一个只把 Plus 额度用完）
- **少数派 0 条的空白首次填补**：60 元蓝牙遥控器当语音输入（**真正收益不是效率而是"长 prompt 明显更愿意说了"**）

### 3. 官方与海外路（23 条，详见 `_r25/_r25_official.md`）

**压缩 / 上下文 / 记忆**：Anthropic Messages API 服务端压缩（**150k 阈值 / 最小 50k / 额外一次采样**）·
五云 memory API 对比（含价格与隔离模型）· GPT-6 Astra 105 万上下文辨析（**272K 计价台阶** + 三层架构）·
harness 空载账单实测法（**30,271 → 2,441**）

**VSCode**：**1.137** 版本本体（**Automations 定时 agent 任务进 Preview** + Voice Mode 实验性）

**模型上新与榜单**：9 月发布日历（**一个月 28 个模型 / 11 天 / 输出单价差 42×**）·
AA **v4.2** 方法论改版（**40% 权重改私有 held-out 集**，v4.1 是 20%；删 GPQA Diamond，增 AA-Briefcase 与 GDP.pdf）·
GPT-Rosalind（生命科学专模 + 4 个自有基准，**token 更少同时准确率更高**）

**DSH**：插件生态规模（**精选 3,632（09-15）→ 社区站收录 16,003（09-21）**；分类里**记忆 173 个**）·
**v0.1.5-rc.1 与 v0.1.6-alpha.2 两个版本节点**（**Session V3 是主要迁移边界、升级后旧版读不了**）

**游戏 / 引擎 / 世界模型**：UE 5.8 实验性 MCP 插件（**「给连接，不给助手」**；UE6 EA 目标 late 2027）·
Unity AI（Muse + Sentis 合并，**走引擎内积分计费**；生成资产带 AI 元数据但责任仍在开发者）·
Ramen Aura 1.0（**Verification Agent 快 8 倍 / 跨会话项目记忆**，$10/月 Indie 档）·
世界模型两条路（**World Labs Atlas = 可导出的 3D vs Google Genie 3 = 可游玩的世界**）·
GDC 2026 调查（**36% 在用 / 52% 认为负面影响**）· 字节空间视频世界模型（⚠️ 单源 + 自述 AI 生成，低置信）

**安全 / 隐私 / 风险**：arXiv 2606.23130（**9,041 个 / 91% / 65.8% 高危**）· arXiv 2609.09560（**快 27% 但更脆**）·
Simon Willison「在 agent 会话里做原创研究**可能并不私密**」（**沉默 ≠ 否认**）·
MCP 的四条价值 + `llm-keys-ui`（**不把 key 贴进会话**）· OpenAI 终止与 Cursor 合作（⚠️ 单源待核）

### 4. 本辑新增「下辑备选池」（4 条，未收录）

`editor-code-assistant/eca` · `specstoryai/getspecstory` · `arabold/docs-mcp-server` · `objectstack-ai/objectstack`
—— 查重 0 命中，但与「16GB Windows + Ollama + 索引站」画像关联度弱于已收条目。

---

## 六、下一辑待办（按优先级）

1. **`/skill-doctor`** —— 连续四轮顺延，而被「307 例：加了『对口』的 skill，agent 反而变差」催得更急。
2. **装 `ollama-vscode` + 跑 `agnix`** —— 本版判定的「最该先试两条」，本轮只登记未装；
   `agnix` 若好用，正好接上房规 #33「规则要能被测」。
3. **做一次 `consolidation`（记忆合并与淘汰）** —— 本辑两篇独立材料同时点名这一环，我们从未做过：
   历史存档 v1~v23、索引 1157 卡、`mistakes/` 27 条，**只增不 merge**。
4. **把「禁止在拿不到工具结果时生成数值类结论」写成一条房规**（含检测器或写明为什么判不了）——
   来源：本辑中文路 #24（线上静默失败）。我们的调研 agent 最容易犯。
5. **房规继续瘦身** —— 现 **22 条 / 191 行**，离 200 行只剩 **9 行余量**；
   下一辑该**压缩每条的正文**（不是再撤条款）。
6. **「预算扫线」为什么做不了，本辑查清了**：我们只有一个测量点（固定前缀 20,900），
   而扫线需要**自变量**。可做的替代是「**按 MCP 开关组合各量一次**」——
   但需要能改本机 MCP 配置并重启会话，**属于要用户配合的动作**，列为待办而不是自测项。
7. **索引条目加「最后复核日期 + 是否作废」**（`mistakes/` 已有，1157 张卡没有）—— 承接第五轮。
8. **给 `insert_vN_cards.py` 家族补一条「P0 语法 + 引号体检」的常驻前置** ——
   本轮 P0 是在探针里加的；直接写在 v31 脚本模板里更省事。
9. 顺延老账：`arch_boundary_probe` 换跑法 / 「命令可跑性」检查 / `rpycdec` 反编译自查（**欠八轮**）/
   Codex `config.yaml` → Ollama 纯离线 / 词表落地 / 模型分档旋钮 / `_tools` 与 `AGENTS.md` 纳入版本控制。
10. **仍以「本机是否真的不跑 dsh」为前提**：本辑 gh 路收了 4 个 dsh 插件（其中 `dsh-win32` 直击 Windows 痛点），
   若决定不跟 dsh，这批只能当观测数据；若跟，**先按 `dsh-win32` 走原生 Windows 路线**，
   并记住 **Session V3 不可回退、升级前先备份**。
11. **`MEMORY.md` 回注预算（R8）**：本辑实测 **3,292 → 2,957 token**（上限 3,000，余量 43）。
   压缩杠杆是**去重**：§三「落盘/沙箱/下载源」与 `AGENTS.md` §一~§三 **逐字重复** → 改成指针；
   其余为 §一 闸门段 / §二 Ren'Py 口诀（转指针）/ §四 / §五 逐行压缩。
   **下次再涨就砍 §六**（当前最长的一节）；顺带确认 **R8 用「汉字 1 / 其它 0.25」估 token**，
   所以**砍中文比砍代码更省**。

---

## 附录 · 本轮落盘与统计

**索引**：`index.html` **1153 → 1168**（第三十版增补 **15 卡**，脚注 [1153]-[1167]）；
卡片 **1142 → 1157**；分组 **299 → 300**；div **8863 → 9105**（平衡）；脚本 `insert_v30_cards.py`。
> ⚠️ E7 快照已重封（`index_metrics.py --update`，300 组）—— 每轮增补后必跑，否则下轮 E7 会把本轮的
> 新增分组报成「需要重封」，把真正的卡数变化淹没在噪声里。

**文档**：
- `csdn-social-summary.md` 本文件（第二十五辑，原文存档 `csdn-social-summary-v23.md` = 第二十三辑）
- `agent-house-rules.md` —— ✅ **22 条 / 191 行**（补写成因说明后 **193**；本辑撤 #40/#43/#44/#46/#47/#48 六条；
  原文逐字进 `-archive.md`，归档表 24 → **30 行**）；`agent-house-rules-enforcement.md` 重新派生（**6 已接线 / 16 只能人判**）
- `AGENTS.md` §四 新增「阈值锚点」（135k 自动压缩 / 20,900 固定前缀）+ 一条「判据的前提要写出来」提醒；
  §七 指针全表同步（1168 项 / 第二十五辑 / M-0027 / E1–E7+W3 / 探针 8/8 / **房规 22 条，行数改"现测"不写死**）
- `.workbuddy/memory/MEMORY.md` —— **R8 曾超预算**（**3,292 → 2,957 token**，上限 3,000）：
  §三 与 `AGENTS.md` §一~§三 逐字重复 → 改指针；§一 闸门段 / §二 Ren'Py 口诀 / §四 / §五 逐行压缩
- `.workbuddy/memory/mistakes/M-0021-claim-not-persisted.yml` —— 补记**第二种形态**（子 agent 汇报"已同步"但文件未改）；重新 `seal` → `check` 27/27

**工具（本辑新修 3 个，全部经反例验证）**：
- `_tools/insert_guard_probe.py` —— 基准自动回溯 + 期望值解析失败必须报红 + **P0 语法前置**
- `_tools/index_metrics.py` —— E7 显式前提 **W3**；`--selftest` **8/8**
- `_tools/gate_recall_probe.py` —— fixture 满足判据前提 + 配一致快照；**8/8**

**错误记忆**：`mistakes/` **25 → 27**（+**M-0026** 判据输入必须校验 / +**M-0027** 负对照必须满足判据前提）；
另有 **16 条**补齐 `why` 字段 → `why` 缺口 **0**；`seal check` **27/27**。

**统计**：本辑新增 **75 条**信源 → 累计经验帖 **约 920 → 约 995 条**
（注：这是**按本辑精选后的入账口径**；原始候选池共约 310 个，三路各自剔除了大量重复与内容农场）。

**三路各自的诚实边界（摘要）**：
- gh：**Gitee / HuggingFace 未覆盖**（`gh` 检索不到 Gitee）；独立成条的「失败记忆 / memory health check」专项**未找到**。
- cn：**VSCode 主题 0 条**（唯一候选批次 `weixin_425xxxxx` 判为内容农场同源）；
  **知乎 / 机器之心 / 量子位 0 条**（仍未解决 v23 提的「宜站内检索」）；
  10/11 条 CSDN 条目出自 `weixin_` 数字账号，**整体存在内容农场嫌疑**，保留标准是
  「不同题不同日期 + 含可核数字 + 无引流外链」三条同时满足。
- official：**官网一手源达成率约一半**（其余为权威二手转述，每条已标来源性质）；
  **5 条单一来源需官方复核**（Grok 4.7 且两源日期冲突 / UE 5.8 / Atlas vs Genie 3 / 字节世界模型 / OpenAI 终止 Cursor 合作）；
  **r/LocalLLaMA 与 HN 原始线程未逐页打开**（主动放弃，不是遗漏）。
